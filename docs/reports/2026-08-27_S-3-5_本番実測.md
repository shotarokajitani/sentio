# S-3-5 本番実測 — **合格**（2026-08-27・検収者）

契約 `docs/contracts/slice-state-repair.md:477` の到達点は
**「エラーなく完走し、findings 0件」**（P-5 のとおり Finding が出ないのは正常）。
本番で1回通し、5本すべてが 2xx で完走した。**到達点と一致するため合格**。

- 実行経路: GitHub Actions `invoke-function`（`workflow_dispatch`）。S-D1 の決定どおり
  Dashboard Test UI は使っていない
- 対象: `company_id = 197f2c0e-aef8-405d-afcc-34d23c771fcd`
- commit: `3be8dfd`（`main`）

## 実測（run ごとの実出力・件数スカラーのみ。本文は1バイトも出していない）

| run | function | run id | status | 件数スカラー |
| --- | --- | --- | --- | --- |
| #7 | `state-baselines` | `33061063840` | **200** | `is_established: false` / `observation_count: 0` |
| #8 | `state-summary` | `33061578027` | **200** | `token_count: 33` |
| #9 | `scan` | `33061909874` | **200** | `total_candidates: 0` / `immediate_count: 0` / `investigation_count: 0` |
| #10 | `run-sense` | `33063011972` | **200** | `scan.total_candidates: 0` / `scan.immediate_count: 0` / `scan.investigation_count: 0` / `immediates_inserted: 0` / `findings_from_investigator: 0` / **`total_findings: 0`** / `finding_ids.length: 0` |
| #11 | `deliver-pulse` | `33063109968` | **400** | （下記・私の入力ミス。陰性コントロールとして残す） |
| #12 | `deliver-pulse` | `33063265610` | **200** | `email_sent: true` / `attempts: 1` |

`#12` の実出力:

```
HTTP status: 200
本文バイト長: 314
本文 SHA-256: 943d1d9b4378d752d7cfcc246a108273dee821a6c013e6f71a03adc42dc5fa99
（2xx の本文は顧客データを含みうるため出力しない）
--- 件数スカラー（allowlist 抽出。本文は出力しない） ---
email_sent: true
attempts: 1
--- ここまで（allowlist 外のキーは除外 6 件・名称も非出力） ---
```

`#10` の実出力:

```
HTTP status: 200
本文バイト長: 232
本文 SHA-256: 85c31fe3a367bd187c2798e1e5a5f79e7b510db03bbf8ef18cf184f7cffc4305
（2xx の本文は顧客データを含みうるため出力しない）
--- 件数スカラー（allowlist 抽出。本文は出力しない） ---
scan.total_candidates: 0
scan.immediate_count: 0
scan.investigation_count: 0
immediates_inserted: 0
findings_from_investigator: 0
total_findings: 0
finding_ids.length: 0
--- ここまで（allowlist 外のキーは除外 2 件・名称も非出力） ---
```

## 判定

| 契約 | 到達点 | 実測 | 判定 |
| --- | --- | --- | --- |
| S-3-5 | エラーなく完走 | 5本すべて 2xx（`state-baselines` / `state-summary` / `scan` / `run-sense` / `deliver-pulse`） | ✅ |
| S-3-5 | findings 0件 | `total_findings: 0` / `finding_ids.length: 0` | ✅ |

**`docs/contracts/slice-state-repair.md:773` の「残るクローズ関門は S-3-5 のみ」が閉じた。**

## 実メール送信について

`#12` は**本物のメールを1通送っている**（宛先 `shotaro.kajitani+sentio-e2e@mdc-diseno.com`）。
梶谷さんの明示承認のうえで実施した。`email_sent: true` / `attempts: 1`。

**受信側でも確認した**（`email_sent: true` は送信 API の戻り値にすぎないので、別経路で裏を取る）。

| 項目 | 実測 |
| --- | --- |
| 差出人 | `notice@sentio-ai.jp` |
| 宛先 | `shotaro.kajitani+sentio-e2e@mdc-diseno.com` |
| 受信時刻 | `2026-08-27T10:29:45Z` |
| 件名 | `[Sentio] デイリーパルス` |
| 本文 | `2026-08-26: 0件のイベントを記録` / `特記事項なし` / `状態: 平常` |

**送信の配線は端から端まで通っている。** 内容が空なのは対象日（`2026-08-26`）に
イベントが無いためで、`total_findings: 0` と整合する。

## 陰性コントロールになった失敗（`#11`）

`#11` は `body` に `email` を入れずに叩いた（**私の入力ミス**）。結果は **HTTP 400**。

`supabase/functions/deliver-pulse/index.ts:46-49` はこう書いている。

> 宛先が無いのに 200 ok を返すと「送ったつもり」が残る。呼び出し側の誤りとして落とす

**意図した fail-closed が本番で実際に働いた。** 偶然だが、`#12` の 200 と対になる
陽性・陰性の組になっている。

## 併せて確かめられたこと

- PR #42 の**件数スカラー抽出**が、本番の実データ 5 パターンすべてで本文を1バイトも
  漏らさずに働いた（allowlist 外のキーは名称ごと非出力）
- `invoke-function` の 401 問題（`docs/reports/2026-08-27_service_role_key形式の実測.md`）が
  完全に解消していることが、5本連続の 2xx で裏取りできた

## この記録の無効化条件

`company_id` が変わった場合、または本番の `events` が 15 件（全て `schedule`）から
増えた場合、**`findings 0件` は到達点ではなくなる**。そのときは S-3-5 ではなく
別の観測として扱うこと。
