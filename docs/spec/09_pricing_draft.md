# 09 プラン設計（**たたき台・未確定**）

**この文書は決定ではない。** ロードマップ第5フェーズ「事業化仕上げ」に入るための
判断材料として、Claude Code が起草した。**梶谷さんが決めるまで、実装には反映しない。**

正本である `06_positioning.md` の数字だけを根拠に組み立てている。
**新しい統計を持ち込んでいない**（対外文書に載せる場合は出典の再確認が要る、という
正本の注記もそのまま効く）。

## 何を売るのか（枠の対象）

`_shared/budget.ts` が既に持っている**唯一の枠**は、フルハーネス調査の日次起動上限である。

```
DEFAULT_PLAN = { id: "default", fullRunsPerDay: 10 }
```

フルハーネス（Planner→Generator→Evaluator）は LLM を最も使う経路で、
**原価がそのまま乗る唯一の場所**でもある。ここを枠にするのは、
売る側の都合（原価）と買う側の理解（調べてくれる回数）が一致する数少ない指標だからである。

**枠にしないもの。**

|                      | 理由                                                                                     |
| -------------------- | ---------------------------------------------------------------------------------------- |
| 取り込むデータ源の数 | 増えるほど製品が効く。絞ると「入力を要求しない」の逆を向く                               |
| 保存期間             | privacy §6 で24ヶ月と公開済み。プランで変えると約束が揺れる                              |
| 配信の回数           | 送るのは Sentio の判断であって利用者の操作ではない。絞ると気づけない日ができる           |
| ライトパス           | `spec/03` が「上限超過はライトパス降格」と定めている。**降格先を絞ると降格が成立しない** |

## 価格の根拠（正本の数字だけを使う）

`06_positioning.md` が置いている代替コストは3つある。

| 代替手段           | 正本の試算                   |
| ------------------ | ---------------------------- |
| 5名×毎日30分の朝会 | **年180万円**（時給3,000円） |
| 5名×毎日15分の日報 | **年90万円**（時給3,000円）  |
| 管理職を1人増やす  | **年600万円規模**            |

**最も小さい代替コストは日報の年90万円である。** ここを上限に置くのが安全側で、
「日報をやめる分で払える」が説明として最も通りやすい。

## たたき台（**数字は仮**）

| プラン   | 月額  | `fullRunsPerDay` | 想定                                                |
| -------- | ----- | ---------------- | --------------------------------------------------- |
| **試用** | 0円   | 3                | 導入判断のため。Day0 と週次が一度は届く量           |
| **標準** | 3万円 | 10               | 現行の既定値と同じ。年36万円で、日報の年90万円の4割 |
| **拡張** | 8万円 | 30               | 拠点や部門が複数ある会社                            |

**3万円の根拠**は「日報の年90万円の4割以内に収める」である。
正本が持っている最小の代替コストに対して、明確に下回る位置に置いた。

**試用を3回にしたのは、`budget.ts` の docstring が
「3 にすると S-3-2（合成会社の一気通貫）が上限に当たる」と警告しているためである。**
テストが素通しできる余裕は既定プラン（10）が持つので、試用は3で構わない。

## 決めていただきたいこと

1. **プランは3段でよいか。** 2段（試用＋標準）の方が売りやすい可能性がある
2. **価格。** 上の3万円は「日報の4割」から逆算しただけで、市場の相場は見ていない
3. **上限に当たったときの見せ方。** `spec/03` の「ライトパス降格」を利用者にどう伝えるか。
   **止まったことを黙っていると、気づかないうちに薄い調査になる**
4. **年額を置くか。** 置くなら Stripe の商品構成が変わる

## 実装の状態

- **A（受け皿）は済んでいる。** `Plan` / `PLANS` / `planFor()` を `_shared/budget.ts` に置いた。
  **いまはどの会社も既定プランを引くので、本番の挙動は変わっていない**
- **B（Stripe）は未着手。** `stripe` は新規依存であり、規約上こちらの一存では入れられない。
  APIキー・Price ID・Webhook 署名シークレット、および Stripe ダッシュボードでの
  商品作成は人間の作業である
- 実装時の既知の罠: `billing_address_collection` / `customer_creation` を渡さない
  （CLAUDE.md の絶対規則。500エラーが既知）
