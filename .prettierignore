# 生成物は整形しない。
#
# pnpm-lock.yaml は pnpm が生成する。Prettier に通すと `'9.0'` → `"9.0"` のように
# クォート種別だけが書き換わり、5061行の無関係な diff が毎回再発する
# （.prettierrc の "singleQuote": false によるもの。lockfileVersion の値は 9.0 のまま）。
# 実害はレビュー不能化であって、pnpm のバージョンドリフトではない。
pnpm-lock.yaml
