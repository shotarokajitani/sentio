# ローカル Claude Code への指示 — スライスD（2026-08-21）

【指示文】の中身をそのまま `C:\Users\shota\sentio` で起動した Claude Code に貼る。

**この PC に Docker は無い。** 実DBを要する検査は CI に委ねる設計になっている。

---

## 【指示文】ここから

Sentio のスライスDを実装する。作業ディレクトリは `C:\Users\shota\sentio`。

まず `CLAUDE.md` と **`docs/contracts/slice-disconnect.md`（契約・active）** を読むこと。
契約の受入基準がこの作業の合否である。契約に無いことをしない。

背景は `docs/reports/2026-08-20_OAuth審査_進行中.md` と
`docs/spec/07_open_items.md:510` 以降。

### 現在の状態（2026-08-21 実測）

| 項目 | 値 |
| --- | --- |
| `origin/main` | `baf592f`（PR #35 squash merge / `deploy #29` success 1m57s） |
| 本番 `/privacy` | **改訂版が出ている**（最終更新 2026-08-20 / §4・§5・§6 表示を実測） |
| 契約 | `docs/contracts/slice-disconnect.md` — **未コミット（`??`）**。このスライスの最初のコミットに含めること |

**`/privacy` §6 は既に公開されている。** 約束と実態がずれている窓が今まさに開いている。
このスライスはその窓を閉じるためのものであり、Google 再提出の前段にある。

### 最初にやること

`origin/main`（`baf592f`）から新しいブランチを切る。既存ブランチに積まない。

### 実装順（**契約の実装順に従う。前後させない**）

1. **D-2（Google 側取り消しの判別）**
   `supabase/functions/_shared/token-refresh.ts` の失敗4経路を書き分ける。
   `tokenRes.ok === false` かつ `400` かつ本文の `error === "invalid_grant"` のときだけ
   `status = 'revoked'` ＋ `revoked_at` ＋ Vault の秘密を直ちに破棄。
   **それ以外はすべて `reauth_required` のまま**（`fetch` の throw / 5xx / 429 / 他の 400）。
   受入基準 D-2-1 〜 D-2-6。**陰性コントロール（D-2-3 / D-2-4 / D-2-5）を先に書く。**
2. **D-1（画面からの解除）**
   `src/app/connect/connect-client.tsx` に解除ボタンと二段確認。
   API は既存の `POST /api/connections/disconnect` をそのまま呼ぶ（**中身を変えない**）。
   受入基準 D-1-1 〜 D-1-5。
3. **D-3（30日経過後の削除）は入れない。** 契約の停止点により、
   **`revoked` が本番で1件観測され、人間が「本当に取り消しだった」と確認するまで着手禁止。**

### 必要になる migration

`connections.revoked_at TIMESTAMPTZ`（`00027`）。`status` は CHECK 制約が無いので
`'revoked'` の追加に migration は要らない（`00007:10` 実測）。
**新規 migration なので `docs/checklists/env-diff.md` を点検し、証跡を残すこと**（受入基準 D-4-2）。

### 判断を仰ぐこと（**勝手に決めない**）

| # | 内容 | いつ要るか |
| --- | --- | --- |
| U-2 | 二段確認の文言（会社名入力 / `DELETE` 入力 / チェックボックス） | **D-1 の着手前** |
| U-3 | `revoked` 検知時にお客様へ通知するか。するなら経路 | **D-2 の着手前** |

**着手前に、案と根拠を並べて止まること。** 実装を進めてから聞かない。

### 検査（Docker 無しで回せるものだけ）

```
pnpm typecheck
pnpm lint
pnpm exec vitest run --exclude 'tests/e2e/**' --exclude 'tests/integration/**'
pnpm run check:db-errors
pnpm run check:caller-guard
pnpm run check:ci-coverage
pnpm run check:edge-types
pnpm run eval:engine
```

`pnpm test`（＝`vitest run`）は使わない。`tests/integration/**` を含み、
ローカル Supabase が無いと落ちる。

**`check:edge-types` は必ず回すこと。** `tsconfig` が各 Function の `index.ts` を
除外しているため、`index.ts` の型検査はこれだけである。前回はここで
`TS2304 Cannot find name 'companies'` を捕まえた。

この環境で**実行できない**検査は `check:allowlist` / `check:schema` /
`supabase db reset`（すべて `requires: live-db`）。`ci.integration` が担保する。
**ローカルが全緑でも完了ではない。判定は CI の色で行う。**

### push 後

CI 全ジョブと PR の Vercel チェックの完了まで自律監視する。赤ならログを取得し、
コードで直せるものは自己修正 → push → 再監視まで自律で回す。
全緑になったら**全ジョブの結果＋実行実態証跡（skip の有無・実行時間）**を添えて報告する。

### 禁止・停止条件

- **merge はしない。** PR を全緑にした時点で止まって報告する
- **`invalid_grant` 以外を `revoked` に丸めない。** 迷ったら `reauth_required`（fail-safe 側）
- **D-3（30日削除）を実装しない。** 契約の停止点
- 削除の件数チェック（`evaluateDeletion` / `MAX_DELETE_ROWS`）を性能を理由に外さない
- `disconnect` API の既存の fail-closed 経路を弱めない
- Vault の秘密をログ・イベント・エラー本文・テストフィクスチャに出さない
- 本番 Ref `kwpldqbnkraftaahnpev` への CLI 直接操作をしない
- 試さずに「できない」と申告しない。実行せずに「完了」と報告しない
- **証跡（ログ引用・実測値）のない完了報告はしない**

### コミット前の確認

グローバル Prettier フック（`~/.claude/settings.json` の `npx prettier --write .`）が
リポジトリ全体を書き換える。前回は13件と10件の無関係なファイルが混入した。
**`git status` を見て、身に覚えのない変更が混ざっていたら戻すこと。**

## 【指示文】ここまで

---

## 補足: 私（検収者）の前回の誤り

`docs/reports/2026-08-21_ローカルCCへの指示.md:25` に
「HEAD `717818c` — **未 push**」と書いたが、**実際には push 済みだった**。
`docs/reports/2026-08-20_現状サマリ.md` の記述を書き写し、
`git rev-parse origin/slice-S-docs2` で照合しなかったことが原因。
誤記は `baf592f` としてリポジトリに入っている。**このスライスのコミットで訂正すること。**
