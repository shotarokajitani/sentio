# 検知 5/7 の内訳を実測した（2026-08-31）

**`07_open_items.md` の見立てが1つ崩れる。**
「④は見つけているが名前が違うだけ」は正しいが、**その名前を直すと 4/7 に下がる。**

## 何を測ったか

`scripts/generate-synthetic-company.ts` の合成会社に `runScan` を当て、
**仕込み信号ごとに、証拠イベントが交差した候補の `scanType` を突き合わせた。**
評価スイート（`tests/eval/engine.test.ts`）と同じ突合を、内訳が見える形で出しただけである。
Scanner は1行も触っていない。

```
=== 仕込みと実際 ===
× #1 order_interval_elongation 期待=trend     実際=[deviation]
     └ Revenue 50000 outside range [70500, 130500]
○ #2 payment_overdue          期待=deadline   実際=[deadline]
○ #3 reply_delay              期待=deviation  実際=[deviation]
     └ Reply time worsening: 2 → 18 (3 consecutive points)
× #4 overtime_creep           期待=trend      実際=[deviation]
     └ Overtime hours increasing: 1 → 3 (5 consecutive points)
× #5 seasonal_normal          期待=none       実際=[なし]      ← 陰性コントロール。正しい
○ #6 inquiry_decline          期待=deviation  実際=[deviation]
     └ Inquiry count declining: 10 → 1 (4 consecutive points)
○ #7 meeting_silence          期待=silence    実際=[silence]
○ #8 competitor_hire          期待=external   実際=[external]

=== どの仕込みにも交差しない候補（＝誤検知） ===
  trend: Rising trend over 4 periods
```

候補の内訳は `{"deviation":8,"trend":1,"silence":1,"deadline":1,"external":1}`。

## **崩れた見立て: ③④⑥ は同じ検出器から出ている**

説明文を見れば分かる。

| 仕込み | 説明文 | 出所 |
| --- | --- | --- |
| #3 reply_delay | `Reply time worsening: …` | `scanMetricChange` の抽出器 `communication` |
| #4 overtime_creep | `Overtime hours increasing: …` | `scanMetricChange` の抽出器 `attendance` |
| #6 inquiry_decline | `Inquiry count declining: …` | `scanMetricChange` の抽出器 `web` |

**3件とも `scanMetricChange`（`src/sense/scanner.ts:194-236`）の出力である。**
この関数は直近5点が**すべて同方向に動いているか**を見ており、
説明文にも `(N consecutive points)` と書く。**現象としては trend である。**
にもかかわらず 226行目は `scanType: "deviation"` と名乗る。

ところが**仕込み側の期待は割れている。**#3 と #6 は `deviation` を期待し、#4 だけ `trend` を期待する。

> **したがって「④を trend と名乗らせる」を単独で行うと、④は○になるが③と⑥が×になる。
> 5/7 → 4/7 に下がる。** `07_open_items` の判断1は、この副作用込みで選ぶ必要がある。

## **#1 はラベルの問題ではない。検出器が存在しない**

`scanTrend` は `event_type === "transaction"` の **`metrics.revenue` の値**しか見ない。
`scanMetricChange` の抽出器は `communication` / `web` / `attendance` の3つだけである。

**発注間隔（イベントとイベントの間隔）を見る検出器が1つも無い。**
だから #1 は金額の外れ値として `deviation` に拾われるだけで、
**どの案を採っても trend にはならない。7/7 には新しい検知の追加が要る。**

## 到達点の見通し（合格線は6）

| 案 | 内容 | 結果 |
| --- | --- | --- |
| **A** | 仕込み #4 の期待を `deviation` に揃える（同じ検出器なら同じ型を期待する） | **6/7** |
| **B** | `scanMetricChange` を `trend` に改名し、#3 #6 の期待も `trend` に揃える | **6/7** |
| **C** | 検出器を分割する（値の外れ＝deviation / 連続の動き＝trend） | **6/7**。設計としては最も正しいが作業が重い |
| — | ④だけ改名する | **4/7（悪化）** |

**合格線6はラベルの整合だけで届く。** #1 を足して 7/7 にするのは別の作業である。

## 誤検知1件の正体

```
trend: Rising trend over 4 periods
```

`scanTrend` が**正常データの revenue** で4連続上昇を拾っている。
`TREND_MIN_PERIODS = 4`（=5点）は、上下する売上に対して低すぎる。
案Bを採ると `scanMetricChange` 由来の候補も `trend` を名乗るため、
**誤検知の見え方も変わる。**閾値と併せて見る必要がある。

## 併せて見つけた欠陥（未修正）

`scanTrend` の `trendIds` は、**方向が反転したときにクリアされない**（`scanner.ts:92-110`）。

```ts
if (curr < prev) { consecutiveDown++; consecutiveUp = 0; trendIds.push(...); }
else if (curr > prev) { consecutiveUp++; consecutiveDown = 0; trendIds.push(...); }
else { consecutiveDown = 0; consecutiveUp = 0; trendIds.length = 0; }  // 等値のときだけクリア
```

カウンタは 0 に戻るのに証拠IDは積まれ続けるので、
**発火したとき `evidence_event_ids` に逆方向の期間のイベントが混ざる。**
評価スイートは証拠の交差で採点するため、**この欠陥は検知数を実際より多く見せうる。**
今回の run では誤検知候補がどの仕込みとも交差しなかったので影響は出ていないが、
**データが変われば出る。**

## この記録がしていないこと

- **Scanner を直していない。** どの案を採るかは `07_open_items.md` の判断待ちであり、
  そこには「勝手に確定させない」と書いてある
- **`07_open_items.md` を書き換えていない。** 判断1の記述（「④は名前が違うだけ」）は
  この実測で不正確になったが、同ファイルはスライスCH のブランチでも編集中で衝突する。
  **CH がマージされた後に追記すること**
