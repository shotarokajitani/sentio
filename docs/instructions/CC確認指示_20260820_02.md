# CC への確認指示（2026-08-20 / 第2便）

作業に入る前に、下記【1】を読んでから【2】の7問に答えてください。
**答えるまでコードを書かないこと。** 実装の指示はこの後で出します。

---

## 【1】検収側で実測した結果。あなたの調査は不要になりました

前便【E】で調査を依頼した件は、こちらでリポジトリの現物を見て決着しました。
**やり直さないでください。** 以下は仮説ではなく実測です。

### 1-1. 作業ツリー3ファイルは全て Prettier。pnpm のバージョンドリフトではない

| 根拠 | 実測値 |
| --- | --- |
| lockfileVersion | `'9.0'` → `"9.0"`。**値は 9.0 のまま。クォート種別だけが変化** |
| `.prettierrc` | `"singleQuote": false`。上の変化と一致する |
| `.prettierignore` | **存在しない**。よって pnpm-lock.yaml は Prettier の対象になる |
| `package.json:24` | `"packageManager": "pnpm@11.21.0"`。**既に固定済み** |
| `.github/workflows/ci.yml:18` | `pnpm/action-setup@v6`（版指定なし＝packageManager を読む）。CI と手元は一致する |
| `src/i18n/ja.ts` | 3行が printWidth:100 で結合されただけ。意味変化なし |

前便の判定フロー「(1) diff に lockfileVersion の行が出ている → バージョンドリフト」は、
**行は出るが値は同じ**という場合を踏み抜きます。この基準は破棄してください。
`packageManager` フィールドで固定する案も不要です（既に入っています）。

### 1-2. `docs/checklists/env-diff.md` は Prettier で実際に壊れている

コードフェンス内のインデントが 14 → 34 スペースに増殖し、
**閉じフェンスと version() 実測欄の表が巻き込まれています。**
表がテーブルとしてレンダリングされない状態です。整形ではなく破壊です。

### 1-3. `00023` は既に存在し、`4917e3d` でコミット済み

`supabase/migrations/00023_baselines_natural_key.sql` を確認しました。
`NULLS NOT DISTINCT` 使用、`server_version_num < 150000` での `RAISE EXCEPTION` 版ガード、
重複の事前検査、RLS 再アサートまで入っています。

したがって前便【H】の「00023 の欠番を埋めること」は**済んでいます**。
version() 待ちは 00023 を*書く*ためではなく、*本番適用で落ちないことを確かめる*ためでした。
本番 17.6 なので通ります。

また、前便【H】に書いた列順 `(company_id, entity_id, metric_key, granularity)` は
**私の誤記です。** 実物は `(company_id, metric_key, entity_id, granularity)` で、
`env-diff.md` の記述とも一致しています。UNIQUE の意味論も `ON CONFLICT` の推論も
列集合で決まるため実害はありません。**実物を指示文に合わせて書き換えないでください。**

### 1-4. `check:schema` は落ちる構成である

`ci.yml:109-111`、integration ジョブ内。`if: ${{ !cancelled() }}` 付き、
`continue-on-error` なし。握りつぶしていません。
→ A-2 の前半は確認済みです。残りは【2】Q2 で聞きます。

---

## 【2】答えてほしいこと（7問）

### Q1. workflow_dispatch を merge 前に実行できない理由（前便 A-1）

「ワークフローファイルがデフォルトブランチに存在しないとディスパッチできない」
という理解で合っていますか。**Yes / No** と、その根拠（GitHub のドキュメントか、
実際に叩いて返ってきたエラー本文のどちらか）を出してください。
記憶ではなく出典か実測で答えること。

### Q2. `4917e3d` 時点の CI run（前便 A-2 の残り）

`check:schema` が落ちる構成である以上、S-5-1 の検査器が赤なら
その run も赤だったはずです。

- 該当 run の **URL**
- **integration ジョブの結果**（緑/赤/skipped のどれか）
- `check:schema` ステップの**結果と出力**

skipped だった場合は、**何が skip の条件になっていたか**を
ワークフロー定義から引用してください。

### Q3. S-5 はなぜ「完了」と報告されたのか（前便 D、未回答）

S-5-1 の検査器は着手前の `4917e3d` 時点で赤でした。それでも完了と共有されています。
何を見て完了と判断したのかを、そのとき実行したコマンドと出力で答えてください。
「確認漏れ」で片付けないこと。**どの検査を回して緑を見たのか**が要ります。

### Q4. 検査器と CI ジョブの照合（前便 D 後半、未回答）

「検査器はあるが CI で落ちない」形が2度出ています
（deno check 28件 / S-5-1）。3点目を探すため、
**検査器の一覧**と **CI で実行されているジョブ・ステップの一覧**が
集合として一致しているかを機械照合するスクリプトの**要否と概略**を答えてください。

**実装はしないこと。** 要否の判断と、作るなら何を突き合わせるかの設計だけ。

### Q5. `budget_usage` の行の初期化経路（前便 J、未回答）

本番の `budget_usage` は **0行**です。
`canRunFullHarness` は `budgetRow?.full_runs ?? null` を渡し、
行が引けなければ fail-closed で起動しません。

- Q5-1. 本番でフルハーネスを初回起動するとき、`budget_usage` の行は
  どこで作られますか。**コード上の該当箇所をファイル名:行番号つきで引用**してください
- Q5-2. 作る経路が存在しない場合、S-3-5（本番データでの完走）は
  予算ゲートで止まります。扱いの案を出してください（**実装はしない**）

fail-closed の設計自体は正しいので、**緩める方向の案は出さないこと。**
「行の初期化を誰がいつ行うか」を決める問題として扱ってください。

### Q6. `07_open_items.md` の既登録2件（前便 ①、未回答）

該当2行を**そのまま引用**してください。申告だけでは受け取りません。
照合する文言は次の2点です。

- remote import →「esm.sh 単一障害点と同型」
- テスト宛先 →「ブロッカーではない」

ずれていれば直してください。

### Q7. `docs/runbooks/2026-08-19_pipeline-first-run.md:94`（前便 F）

「Dashboard → Invoke / Test タブ」を workflow_dispatch に差し替える件は
対応済みですか。済んでいれば diff を、未対応なら未対応と答えてください。

---

## 【3】答えると同時にやってよい作業（これだけ）

`env-diff.md` が壊れたままだと本番 PostgreSQL の実測値を書き込めないため、
ここだけ先に片付けてください。

1. 3ファイルを `git checkout --` で戻す
   （`docs/checklists/env-diff.md` / `pnpm-lock.yaml` / `src/i18n/ja.ts`）
2. 戻したあと `git status --porcelain` を貼る
3. `env-diff.md` の version() 実測欄が**表として復元されている**ことを確認して貼る
4. 未追跡の `.playwright-mcp/` を片付ける
   （CLAUDE.md 常設指示「検証プロセスの後始末」）。
   削除するか `.gitignore` に入れるかは、それが何を出力したものかを見て決め、
   **どちらにしたかと理由を報告**すること
5. `.prettierignore` を新規作成し、`pnpm-lock.yaml` を除外する
   （生成物を整形対象にしている限り同じ 5061 行の diff が再発するため）

**実測値の記入はまだしないこと。** 表が戻ったことを確認したら、
記入内容はこちらから渡します。

---

## 【4】着手しないもの

- S-1 の本番適用（`db push`）
- S-3
- merge
- Q4 のスクリプト実装
- Q5-2 の実装
- `00023` の書き換え（1-3 のとおり、現状で正しい）
