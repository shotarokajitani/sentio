# ローカル Claude Code への指示 — スライスW（2026-08-27）

【指示文】の中身をそのまま `C:\Users\shota\sentio` で起動した Claude Code に貼る。

**この PC に Docker は無い。** 実DBを要する検査は CI に委ねる設計になっている。

---

## 【指示文】ここから

Sentio のスライスWを実装する。作業ディレクトリは `C:\Users\shota\sentio`。

まず `CLAUDE.md` と **`docs/contracts/slice-weekly-report.md`（契約・active）** を読むこと。
契約の受入基準がこの作業の合否である。契約に無いことをしない。

背景は `docs/reports/2026-08-27_デモ動画_シーン4が撮れない.md` と
`docs/runbooks/2026-08-20_google-oauth-verification.md`【2】シーン4。

### 現在の状態（2026-08-27 実測）

| 項目 | 値 |
| --- | --- |
| `origin/main` | `3be8dfd`（PR #43 squash merge / slice-D） |
| ローカルの HEAD | `308d4b2`（ブランチ `slice-D-disconnect`）。**main より古い。積まない** |
| スライスS | **クローズ済み**（S-3-5 合格・`docs/reports/2026-08-27_S-3-5_本番実測.md`） |

### 最初にやること

**`origin/main`（`3be8dfd`）から新しいブランチを切る。`slice-D-disconnect` に積まない。**

作業ツリーに検収者（Cowork）が書いた**未コミットの変更が4件ある。**
**このスライスの最初のコミットに含めること。** 内容は読んで確認すること。

| ファイル | 種別 | 中身 |
| --- | --- | --- |
| `docs/reports/2026-08-27_S-3-5_本番実測.md` | 新規 | S-3-5 の合格記録（run 5本の実出力・受信メールの裏取り） |
| `docs/reports/2026-08-27_デモ動画_シーン4が撮れない.md` | 新規 | このスライスの発端 |
| `docs/contracts/slice-weekly-report.md` | 新規 | **この作業の契約** |
| `docs/reports/2026-08-27_ローカルCCへの指示_sliceW.md` | 新規 | この指示文 |
| `docs/contracts/slice-disconnect.md` | 変更 | D-1-1 を達成に更新。本番実測とhydration不具合を追記 |
| `docs/secrets-runbook.md` | 変更 | 新形式キーが正である旨の訂正。ゲートウェイ二層の記述を取り消し |
| `docs/reports/2026-08-27_service_role_key形式の実測.md` | 変更 | 宿題2件を消し込み |

### 実装順（**契約の実装順に従う。前後させない**）

1. **`src/lib/report/weekly.ts`（純関数）から書く。**
   入力は `events` の行の配列、出力は集計結果。DB にも Next にも依存させない。
   **陰性コントロール（W-1-3 / W-1-5 / W-2-1 / W-2-4）を先に書く。**
   - W-1-3: 終日予定（24時間）を総会議時間に混ぜない
   - W-1-5: 前週0件でゼロ除算も「0%」も出さない
   - W-2-1: **出席者のメールアドレスが出力に1文字も出ない**
   - W-2-4: `google_calendar` 以外の `source` を会議として数えない
2. `src/app/report/page.tsx`（Server Component）＋表示コンポーネント。
   **`src/app/connect/page.tsx` と同じ形にする**（`getAuthedContext()` → RLS クライアント →
   `src/lib/...` の関数 → クライアントへ渡す）。生の内部コードをクライアントへ渡さない
3. `/connect` から `/report` への導線を1本だけ足す

### 触ってはいけないもの

- **新 migration を作らない**（契約 W-D6）。作りたくなったら**止めて相談する**
- `metrics.attendees` の中身を画面・ログ・エラー本文・テストフィクスチャに出さない。
  **テストで使うメールアドレスも実在しない値にする**
- `baselines` を使わない（契約 W-D2）。`is_established: false` / `observation_count: 0` で、
  まだ比較の土台になっていない
- デイリーパルスのメール本文を変えない（案B は採らなかった）

### 検査（Docker 無しで回せるものだけ）

```
pnpm typecheck
pnpm lint
pnpm exec vitest run --exclude 'tests/e2e/**' --exclude 'tests/integration/**'
pnpm run check:db-errors
pnpm run check:caller-guard
pnpm run check:ci-coverage
pnpm run check:edge-types
pnpm run eval:engine
```

`pnpm test`（＝`vitest run`）は使わない。`tests/integration/**` を含み、
ローカル Supabase が無いと落ちる。

この環境で**実行できない**検査は `check:allowlist` / `check:schema` /
`supabase db reset`（すべて `requires: live-db`）。`ci.integration` が担保する。
**ローカルが全緑でも完了ではない。判定は CI の色で行う。**

### push 後

CI 全ジョブと PR の Vercel チェックの完了まで自律監視する。赤ならログを取得し、
コードで直せるものは自己修正 → push → 再監視まで自律で回す。
全緑になったら**全ジョブの結果＋実行実態証跡（skip の有無・実行時間）**を添えて報告する。

**`jq` をローカルの監視ループで使わないこと**（不在で空洞化した前例が
`.claude/skills/gotchas` にある。自分で記録したものである）。

### 禁止・停止条件

- **merge はしない。** PR を全緑にした時点で止まって報告する
- 本番 Ref `kwpldqbnkraftaahnpev` への CLI 直接操作をしない
- Vault の秘密をログ・イベント・エラー本文・テストフィクスチャに出さない
- 試さずに「できない」と申告しない。実行せずに「完了」と報告しない
- **証跡（ログ引用・実測値）のない完了報告はしない**

### コミット前の確認

グローバル Prettier フック（`~/.claude/settings.json` の `npx prettier --write .`）が
リポジトリ全体を書き換える。**`git status` を見て、身に覚えのない変更が混ざっていたら戻すこと。**

## 【指示文】ここまで

---

## 補足: このスライスの後に何が起きるか

1. 検収者（Cowork）が本番の `/report` を見て、シーン4として撮れるかを判定する
2. デモ動画の撮影（runbook【2】の台本・シーン1〜6）
3. runbook【3】の英文の書き直し。
   **現行の「the dashboard」「Weekly report section」は存在しない画面を案内している**
4. 審査用アカウントの作成（梶谷さん・エージェントは触らない）
5. Google へ再提出

## 補足: 未処理で残っているもの（このスライスの範囲外）

- **`/connect` の hydration mismatch（React error #418）。**
  `src/app/connect/connect-client.tsx:117` の `toLocaleString("ja-JP", …)` に
  `timeZone` が無く、サーバ（UTC）とクライアント（JST）で9時間ずれる。
  **`/report` で日時を出すときに同じ形を作らないこと。**
  新しく書く側では最初から `timeZone: "Asia/Tokyo"` を明示する
- cron の実疎通確認（停止点 2-b）。人間が SQL を叩く
- `docs/spec/07_open_items.md` のレガシー JWT 項目の書き直し（**人間の判断待ち**）
