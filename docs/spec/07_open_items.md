# 07 未確定・要確認キュー（勝手に確定させない）

| 日付       | セッション | 変更概要                           |
| ---------- | ---------- | ---------------------------------- |
| 2026-07-23 | #01        | セッション#01由来の未確定3項目追加 |

## 事業判断待ち（人間）

- プラン具体値（原則のみ確定: 体験×調査予算×Finding属性範囲で切る。「連携数課金」は禁止）
  前提条件（禁じ手9・確定）: 機構の2分——【担保＝全プラン標準】3点開示/透明性ページ/本文非取得/
  カバレッジ・鮮度・確度の正直表示/証拠遡及/ケア文脈制限/勝手に送らない/fail-closed/進行中損失の即時通知
  【価値＝課金軸】調査予算/「Sentioに聞く」/月次レーダー/Finding属性範囲/非損失系の即時性
  （下位プランは週次集約で必ず届く＝隠さない、遅く届くだけ）
- spec/10 暫定A/B/Cの承認
- オンボーダー採算モデル（初期10〜30社は獲得コストとして許容、以降の設計）
- Phase3 横断パターンの匿名化設計（法務含む）
- パターンE・F（チャット非依存シグナル）の閾値・検出ロジック詳細（→03。2026-07-23 #01で検出元対応表まで確定済み）
- Microsoft 365コネクタの詳細仕様（指示書 #02 の調査結果待ち。→01）
- 価格軸のQuestion頻度設計の確定（カバレッジ軸は不成立。→04）

## 規約・API要確認

- 勤怠（KOT・ジョブカン・freee人事労務）の外部利用可否【優先度先頭・個人沈黙シグナルの解禁条件のため】
- Google Workspaceドメイン委任の審査要件
- 取引先実名のFinding内露出範囲（共有・転送時）
- Resend差出人のsentio-ai.jp本番化（SPF/DKIM）
- Chatworkの過去メッセージ取得可否 / freee口座明細スコープと規約 / Square等POSのAPI・規約
- 人流オープンデータの二次利用条件 / 気象データ商用条件 / LINE公式のinsight API
- GBP API: Lauda側の実地知見で要件確定（2026-07-02）——Basic API Access申請が必須（api_defaultフォーム）、
  申請者自身の検証済み・60日以上アクティブなGBP保有が条件（ディセーノGBPはクリア済み）、提出はGBPオーナー権限アカウント、
  承認判定はクォータ0/300 QPM。承認はGCPプロジェクト単位のため、SentioがLaudaの申請済みプロジェクト（reply-ai）を
  共用するか別プロジェクトで申請するかは設計判断（要確認）
- Instagramレート制限がアプリ単位かアカウント単位か
- SmartHR（Plusアプリ登録）・Jobcan（DONUTS社NDA）・MF勤怠の外部利用可否（既存キュー）
- Supabaseプレビューブランチでのcron/secrets/Vault再現範囲
- **特定商取引法に基づく表記は未対応（2026-08-19 登録・現時点では不要）**。
  無償提供の間は掲示義務が生じないため作っていない。**有償化（Stripeで課金を開始する）
  時点で必須**になり、事業者名・住所・電話番号・代表者・販売価格・支払方法・
  提供時期・返品/解約条件の掲示が要る。プライバシーポリシー §8 で
  「住所・代表者氏名はお求めに応じて回答」としている扱いも、有償化時には
  常時掲示へ切り替える必要がある。**課金スライスの着手条件として扱うこと**

## 申請キュー（リードタイム＝クリティカルパス）

Google審査（進行中・継続）→ Slack Marketplace → Metaアプリ審査（Instagram）→ GBP利用申請。
Gmail CASA・GBPの実装はフェーズ2。BOJ APIは本番公開時にpost.rsd17@boj.or.jpへ通知＋クレジット表示。

## 既存資産の扱い（スライス1契約で切り分け提示）

Stripe本番・認証・ドメイン・Resend・Sentry・登録済みSecretsは流用。旧Edge Functions/スキーマは凍結→新設計で置換。

## OAuth同意画面を本番公開するか（Google審査の要否・2026-08-18 登録）

現在の同意画面は **テスト中（外部）**、テストユーザーは
`shotaro.kajitani@mdc-diseno.com` の1名のみ。

**この設定の直接の帰結: refresh_token が7日で失効する。**

> **2026-08-18 更新。** ここに書いていた検証用接続（2026-08-18 04:41 UTC 作成・
> 2026-08-25 頃に失効見込み）は、スライスA本番切替の手順4で**削除済み**。
> 現在の接続は `985e6672…`（company
> `197f2c0e-aef8-405d-afcc-34d23c771fcd` / `active`）**1件のみ**。
>
> **2026-08-19 再更新。** A-4 実証で一度 `reauth_required` まで落としてから
> 再接続したため、**7日の時計は 2026-08-19 00:29 JST から数え直し**になった
> （失効見込みは 2026-08-26 頃）。以後も再連携するたびに起点は動くので、
> 実際の値は `select last_refresh, expires_at from connections` で確認すること
> （`connections` に `created_at` は無い）。
> 失効すると cron 実行で `status = reauth_required` に落ちる。

- 検証目的では問題ない（B-s2-2 の自然な本番実証として使える）
- **実ユーザーを1社でも迎えるなら成立しない。** 7日ごとに再連携を強いることになる

### 判断が要ること

1. **本番公開（In production）に切り替えるか** — 切り替えれば refresh_token は
   長期有効になる。ただし `calendar.readonly` は Google の**制限付きスコープ**にあたり、
   公開には**審査（およびスコープによっては年次のセキュリティ評価）**が必要になる可能性が高い。
   リードタイムが読めないため、申請キュー（本ファイル別項）と同じ扱いで早めに着手するか判断が要る
2. 審査を通さない場合の代替 — テストユーザー枠（最大100名）での運用に留めるか、
   別の連携方式にするか

**関連する既存項目**: 本ファイル「申請キュー」の Google審査（進行中・継続）。
同一の審査枠で扱えるのか、別申請が要るのかは未確認。

---

## /connect スライスで顕在化した2件（2026-08-18 登録）

### 1. `/api/connections` の未認証アクセス — **クローズ済み（2026-08-18、スライスA）**

**状態: 解消。** 認証スライス（`docs/contracts/slice-auth-ui.md` A-2）で塞いだ。

当時の指摘: `src/app/api/connections/route.ts` は認証を持たず、`company_id` をクエリパラメータで
受け取り、**service_role キー**で `connections` と `events` を読んでいた。
RLS をバイパスするため、company_id を知る／推測できる第三者が
任意の会社の接続状態とイベント件数を取得できた。

対処:

- `GET /api/connections` は引数を取らなくなった。company_id をクエリパラメータで受け取る経路が
  構造的に存在しない。company_id はセッション（`auth.uid()`）から導出する
- service_role をやめ、anon key ＋ ユーザーセッションのクライアントで読む。
  越境はアプリだけでなく RLS でも止まる（二重化）
- 未認証は 401

**調査で判明した追加分**: 同じ「クライアント供給の company_id を無検証で信用する」形は
`/api/connections` だけでなく5本あった。読み取りだけでなく**他社への書き込み**も通っていた。
いずれも同時に塞いだ。

| ルート                    | 当時の受け取り方                    | 影響                                                 |
| ------------------------- | ----------------------------------- | ---------------------------------------------------- |
| `api/connections`         | クエリparam                         | 他社の接続状態とイベント件数の読み取り               |
| `api/csv/ingest`          | JSON body                           | 他社スコープへの events 書き込み                     |
| `api/competitors/suggest` | JSON body                           | 他社スコープへの entities 書き込み                   |
| `api/auth/google`         | クエリparam を OAuth `state` に流用 | 他社への接続紐付け／state がCSRFトークンとして無機能 |
| `api/auth/freee`          | 同上                                | 同上                                                 |

OAuth の `state` は 32バイトの乱数に変え、httpOnly cookie と照合するようにした。

固定テスト: `tests/unit/connections-api-auth.test.ts`（未認証401）、
`tests/integration/connections-api.test.ts`（2ユーザー2社の実クエリによる越境不可）。

### 2. カレンダーの件名・出席者メールを `events.metrics` に保存している（製品判断）

`src/app/auth/callback/google/route.ts` の同期処理は、取り込んだ予定について
`metrics: { title, attendees }` を保存する（`sensitivity: "S1"`）。
件名の文字列と出席者のメールアドレスがそのまま DB に載る。

**両論併記（判断は人間）:**

- **保存してよい側の論拠**: allowlist のカラム名規則には抵触しない（`metrics` は
  JSONB の許可カラムで、禁止パターン `body|content|text|message|subject` は
  カラム名に対する規則）。件名はパターン検出（会議の沈黙・頻度変化）の
  精度に直結し、Sense層の質を上げる。S1 として分類済みで、RLSで会社スコープに閉じている。
- **保存すべきでない側の論拠**: 製品の担保として掲げている「**本文非取得**」に対し、
  会議の件名は実質的に本文に近い。出席者メールは個人情報であり、
  取引先の実名露出（本ファイル「事業判断待ち」の既存項目）とも地続き。
  Findingの根拠提示で件名が画面に出ると、ユーザーの期待と食い違う可能性がある。

**選択肢**: (a) 現状維持 (b) 件名をハッシュ／長さ等の派生値に置換
(c) 出席者はドメインのみ保持 (d) 件名は保持するがFinding表示には使わない

---

## ~~本番public に残存する旧スキーマの処遇~~ → **クローズ（方針A＝削除・2026-08-18 適用完了）**

> **決定: 旧スキーマ16テーブルを削除する（方針A）。2026-08-17 検収者承認。**
> **2026-08-18 に本番適用完了・最終検収合格**（deploy run 32088752964）。
> 適用後確認SQLで全行OK: 旧16件消滅 / 新12件無傷 / 想定外テーブルなし /
> cron は `sync-connections` のみ active / 履歴21件・最新 `00021`。
> 旧cronジョブ7件の実解除もこの実測で確定した。
>
> - 削除は `supabase/migrations/00021_drop_legacy_schema.sql` で実施。明示リストの
>   単一 `DROP TABLE IF EXISTS`（CASCADE不使用。理由は同ファイル内のコメント）
> - ~~バックアップは人間がJSONスナップショットで取得済み~~ →
>   **2026-08-18 訂正: 保存時の取り違えにより、バックアップは実際には取得されていなかった。**
>   削除前データは失われている（影響評価は下記「旧メールジョブ」項を参照）。
>   Supabase Pro の日次バックアップに削除前の復元ポイントが残る点のみ記録
> - 事前調査: `docs/runbooks/2026-08-17_legacy-drop-preflight.sql`
> - 適用後確認: `docs/runbooks/2026-08-17_legacy-drop-postdeploy.sql`
> - `00013` / `00014` の明示リストは**維持**する。旧テーブル回避という当初の動機は
>   解消したが、別の理由（管理外テーブルへの依存を断つ／権限の唯一の台帳）で
>   引き続き正当なため。各ファイルに追記済み
>
> **決定に至った経緯**（判断材料として残す）:
> 実測でテナント越境は無く（下表の評価1・2）、緊急性は無かった。
> それでも削除を選んだのは、共存を続ける積極的な理由が無く、
> `api_keys` が秘密をVaultでなくテーブルに保持する旧設計だったため（評価3-a）。
>
> **旧cronジョブ7件も同時に解除する**（2026-08-17 preflight 実測）。
> `daily-trial-check` / `monthly-cleanup-external-data` / `daily-expire-questions` /
> `daily-expire-signals` / `monthly-delete-expired-companies` /
> `weekly-summary-email` / `daily-onboarding-mail` が **4月から `active` のまま残存**していた。
> 解除しないままテーブルを消すと、以後ずっと静かに失敗し続ける。
> `sync-connections` は新スキーマ側なので解除しない。
>
> **旧メール系ジョブ2件の調査 → クローズ（2026-08-18・検収者判断）**:
> `weekly-summary-email` と `daily-onboarding-mail` が4月から `active` だった。
>
> バックアップJSONは**保存時の取り違えにより実際には取得されておらず、材料喪失**。
> ただし削除前実測で旧DBの実データは**テスト会社1件のみ（実ユーザーゼロ）**と確定しており、
> 仮に送信があっても宛先は開発者自身に限られる。
> **実ユーザーへの送信リスクは構造的に存在しなかった**ため、追加調査は不要と判断。
> Supabase Pro の日次バックアップに削除前の復元ポイントが残る点のみ記録する。
>
> 以下は当時の実測記録。決定の根拠として保存する。

### （記録）当時の状況

診断キットQ1〜Q8（`docs/runbooks/2026-08-12_migration-state-diagnosis.md`）の実測で、
本番 `public` に**16テーブルが残存**していることが確定した（分岐C）。
新スキーマ12テーブルは未作成で、これらと**共存させる前提**で修復を進めていた。

現状の緩和事実（Q2実測）: 16件とも `rls_enabled = true`。
`click_tokens` / `cron_job_logs` / `error_logs` は `policy_count = 0`＝ポリシー不在のため
非superuserからは全拒否（fail-closed）。
`rls_enabled = false` かつ `anon_can_select = true` の即時対応対象は**0件**。

- **旧スキーマ14件**（4月構築・`archive/legacy` 由来と対応）:
  `click_tokens` / `companies` / `competitors` / `conversations` / `cron_job_logs` /
  `external_data` / `industry_patterns` / `integrations` / `notification_logs` /
  `patterns` / `questions` / `signals` / `subscriptions` / `usage_logs`
- **想定外2件**（旧スキーマ台帳にも新スキーマにも無い）:
  `api_keys`（RLS有効・policy 1件） / `error_logs`（RLS有効・policy 0件）
  → 出所と用途が未特定。特に `api_keys` は名称上、秘密を保持している可能性があるため
  **中身の確認と処遇判断を優先する**（確認は読み取り専用SQLで列構成のみ。値は取得しない）

判断が要るのは「削除 / 別スキーマへ退避 / RLS有効のまま残置」の3択。
それまでの間、Sentioのマイグレーションは旧テーブルに**触れない**設計にしてある
（00013の検証対象・00014のGRANT対象をいずれも新スキーマ12テーブルの明示リストに限定済み）。

### ポリシー内容の実測（2026-08-15・`pg_policies` 読み取り）

**「旧ポリシー内容は未検証」という穴はこの実測で閉じた。** 13ポリシー / 13テーブル
（＋ポリシー0件が3テーブル）。

| 構成                                                              | 対象                                                                                                                                                                        | 定義                                                                                               |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| own companyスコープ・`FOR ALL`・roles=`public`・`with_check`=null | `api_keys`("own keys") / `competitors` / `conversations` / `external_data` / `integrations` / `patterns` / `questions` / `signals` / `subscriptions` / `usage_logs`（10件） | `qual = (company_id IN (SELECT companies.id FROM companies WHERE companies.user_id = auth.uid()))` |
| 同上（自テーブルが起点）                                          | `companies`("own company")                                                                                                                                                  | `qual = (user_id = auth.uid())`                                                                    |
| SELECTのみ・roles=`authenticated`                                 | `industry_patterns`（共有マスタ）                                                                                                                                           | `qual = true`。書き込みポリシー**なし**                                                            |
| SELECTのみ・roles=`authenticated`                                 | `notification_logs`                                                                                                                                                         | own companyスコープ。書き込みポリシー**なし**                                                      |
| ポリシー0件                                                       | `click_tokens` / `cron_job_logs` / `error_logs`                                                                                                                             | RLS有効かつポリシー不在＝非superuserから全拒否                                                     |

**評価:**

1. **テナント越境なし。** 全ポリシーが `auth.uid()` 起点のスコープで、`anon` は
   `auth.uid()` が NULL のため実効0行。`authenticated` の書き込みグラント（既存要因）も、
   `FOR ALL` の `with_check` が null で USING式を継承するため自社スコープに制限される。
   **実効的な越境書き込み・NULL書き込みは不可。**
2. **書き込みの実効遮断。** `industry_patterns` / `notification_logs`（書き込みポリシー不在）と
   ポリシー0件の3テーブルは、グラントがあってもRLSが全書き込みを拒否する。
3. **残る負債（緊急性なし・処遇判断の材料）:**
   - (a) `api_keys` はテナント越境こそ無いが、**秘密をVaultでなくテーブルに保持する旧設計**。
     旧スキーマ処遇判断で優先的に扱う
   - (b) 旧ポリシーは `FOR ALL` ＋ WITH CHECK暗黙継承という、新スキーマ監査で指摘され
     `00019` で修正した型と**同型**。ただし qual に NULL許容が無いため実害なし。
     旧テーブルは凍結方針のため**修正はせず、この事実の記録のみ**

~~**未確定のまま維持する点:** 旧スキーマの処遇（削除 / 退避 / 残置）そのもの。~~
→ **2026-08-17 に「削除」で確定（本節冒頭を参照）。この項目はクローズ。**

## `check:allowlist` のCI空洞 — 修正タスク（2026-08-18 登録・本PRでは修正しない）

CLAUDE.md絶対規則「S2テーブルに本文型カラムを追加しない（allowlist外カラムのマイグレーション禁止）」は、
CIジョブ `verify` の `pnpm run check:allowlist` で担保されている**建て付けになっているが、実際には担保されていない**。

**実測（run 32134357004 / job 95702233426）:** このstepの出力は
`check:allowlist — run against live DB` の**1行のみ**。`scripts/check-allowlist.ts` のCLI入口は
`console.log` だけで、実DBの `information_schema.columns` を照会していない。
純粋関数 `validateS2Columns` は `tests/unit/allowlist.test.ts` の3ケースで検証済みだが、
**その関数を実スキーマに当てる経路が存在しない**ため、allowlist外カラムが本番に入っても
CIは必ず緑になる。

### 判断が要ること

1. **どのDBに当てるか。** ローカル（`supabase start`）／CIのSupabaseサービスコンテナ（`integration`
   ジョブは既にDB接続を持ち、8ファイル44テストをskipなしで実行できている）／本番read-only の3択。
   本番Refへの直接操作はCLAUDE.md絶対規則で禁止のため、実行するならCI経由に限る
2. **どのジョブに置くか。** 現状 `verify` にあるがDB接続を持たない。`integration` 側へ移すのが素直だが、
   その場合「S2規則違反の検知」が統合テストの成否に混ざる
3. **失敗時の扱い。** allowlist違反はマイグレーションのmerge阻止条件なので、fail-closed（ジョブ失敗）とする

### 暫定の扱い

修正までの間、S2スキーマ変更は `.claude/skills/migration` の手順と schema-reviewer の
レビューだけが担保になる。**「CIが通ったからallowlistは守られている」と読んではならない。**

## 「取り消し済みだが期限内」の窓で接続済みに見えたままになる（2026-08-19 登録・実装未着手）

A-4 の本番実証（`docs/runbooks/2026-08-18_slice-a-cutover.md` 手順5）で検収者が発見した。

**症状:** 利用者が Google 側で Sentio のアクセスを取り消しても、発行済みアクセストークンは
自身の期限まで有効なままなので、Sentio はリフレッシュに行かない。その間 Calendar API は
**401** を返し取り込みは止まっているのに、`/connect` は**「接続済み」を表示し続ける**。
窓の長さは最長で「アクセストークンの寿命（約1時間）− 期限バッファ5分」。

**原因（実装どおりで、故障ではない）:**

- `markReauthRequired` を呼ぶのは `supabase/functions/_shared/token-refresh.ts` の
  **リフレッシュ失敗経路だけ**（vault read 失敗 / payload不正 / token fetch 失敗 /
  token endpoint 非200 の4箇所）
- `supabase/functions/sync-connections/index.ts` の同期本体が投げた例外は
  末尾の catch で `results` に `status: "error"` を積むだけで、
  `connections.status` を触らない
- 期限判定は `EXPIRY_BUFFER_MS = 5 * 60 * 1000`

**改善候補:** Calendar / freee API が **401** を返したときも `reauth_required` へ落とす。

### 判断が要ること

1. **401 だけを対象にするか。** 403（スコープ不足・APIが無効）や 429 を巻き込むと、
   一時的な事象で「要再連携」を出して利用者に不要な再連携を強いる。
   Sentio は入力を求めない製品なので、**誤検知のコストは取りこぼしより高い**
2. **どこで落とすか。** 同期関数それぞれに置くと重複する。
   `token-refresh.ts` と同じく共有ヘルパーに寄せるのが素直
3. **fail-closed の向き。** 401 を受けた回数で落とすか、1回で落とすか。
   Google 側の一過性 401 を踏むと、1回で落とす実装は誤検知しやすい

**着手前に必要なもの:** 実装より先に、401 を返す接続を作る統合テスト。
A-4 の実証は手作業で1回通っただけで、回帰を止める仕掛けが無い。

## 検収用メールエイリアス `sentio-test@mdc-diseno.com` の作成（2026-08-19 登録・人間作業）

**ブロッカーではない。** 現状はプラス記法 `shotaro.kajitani+sentio-e2e@mdc-diseno.com` で
代替できており、スライスSの進行は止まらない。

**なぜ要るか。** 実送信を伴う検収（`docs/runbooks/2026-08-19_pipeline-first-run.md` の STEP 6、
契約 S-3-2 の一気通貫）の宛先が、個人の受信箱に紐づいたままだと以下が起きる:

- 手順書・フィクスチャ・テストデータに**生の個人アドレスが残り続ける**
- 担当者が変わったとき、検収経路が個人アカウントに依存して引き継げない

**やること:** `mdc-diseno.com` に `sentio-test@` のエイリアス（または共有メールボックス）を作る。

**できたら差し替える場所:**

- `docs/runbooks/2026-08-19_pipeline-first-run.md` の STEP 6 の `email`
- 実送信を伴う検収手順の宛先（`tests/fixtures/recipients.ts` の `HUMAN_VERIFY_RECIPIENT`）

**差し替えないもの:** ローカル / CI の自動テスト宛先は `sentio-e2e@example.com`（RFC 2606
予約ドメイン）のままにする。あの経路は `RESEND_API_KEY` を渡さず**送信しない**ので到達性は
要件でなく、到達するアドレスを焼き込むと**キーが混入した瞬間に実メールが飛ぶ**。

## ~~宛先未設定の会社の扱いは呼び出し元の責務~~ → **クローズ（2026-08-31・スライスCD）**

**決定済み。宛先の正本は `auth.users.email`（契約 `docs/contracts/slice-cron-dispatch.md` CD-D1）。**

RLS ポリシー（`00019`）が `company_id = auth.uid()` で会社とアカウントが 1:1 なので、
`auth.users.id` がそのまま `company_id` になる。**宛先テーブルは作らない。**
別に持つと正本が2つになり、片方が古くなる。

呼び出し元の責務は**ディスパッチャ**（`dispatch-daily` / `dispatch-weekly`）が持つ。

- 宛先が取れない会社には `deliver-*` を**呼ばない**（CD-1-3）。スキップとして集計に載せる
- 連携が1つも無い会社にも呼ばない（CD-1-2）
- したがって**400 は積み上がらない**。この項目が予告していた設計漏れは塞いだ

以下は登録当時の記述（経緯として残す）。

### （記録）2026-08-20 登録時の記述

`deliver-pulse` / `deliver-weekly` / `deliver-alert` / `day0` は、`email` が無ければ
**400 で落ちる**（契約 OUT欄の例外1）。宛先が無いのに 200 ok を返すと
「送ったつもり」が残り、未送信が正常系に化けるためである。

**ただし「会社に宛先が1件も登録されていない」は正常な状態であってバグではない。**
Sentio は入力を求めない製品なので、連携直後や試用中に宛先が空であることは普通に起きる。

**したがって、呼び出し元（cron / パイプライン編成）を作る時点で必ず決めること:**

- 宛先ゼロの会社は `deliver-*` を**呼ばない**。上流で「宛先未設定のため配信しなかった」を
  記録する（`delivery_log` に書くのか、別の経路にするのかは未確定）
- この 400 を「パイプラインが黙って止まる原因」にしない。
  400 が積み上がる状態＝呼び出し元の設計漏れ、として扱う

~~**未確定:** 宛先をどこに持つか（会社ごとの単一アドレスか、受信者テーブルか）。~~
→ **2026-08-31 に確定。`auth.users.email` を正本とする（CD-D1）。** 受信者テーブルは作らない。
複数宛先が要るようになったら、そのときに別の open item として立て直す。

## `deno.land/std` の remote import を外す（2026-08-20 登録・スライスS範囲外）

`ingest-calendar` / `ingest-csv` / `ingest-monitor` / `ingest-s0` の4本が

```ts
import { serve } from "https://deno.land/std@0.177.0/http/server.ts";
```

を **remote import** している（`check:edge-types` を作る過程で判明）。

**なぜ課題なのか。** `esm.sh` 単一障害点（`gotchas`）と同型で、**実行時の単一障害点**である。
`esm.sh` が 522 を返して全 function のバンドルが止まった事故と同じ形が、
`deno.land` の都合で起こりうる。忘れると外部都合で本番が止まる。

**しかもこの依存はもう要らない。** `Deno.serve` は Deno 2 系の**組み込み**で、
他の13本は既にそちらを使っている。4本だけ古い書き方が残っている。

**やること:** 4本の `serve(...)` を `Deno.serve(...)` に置き換え、import を削除する。

**なぜ今やらないのか:** スライスS の範囲は「State層から先が実スキーマに対して動くこと」と
「越境封鎖」であり、ingest 系の中身は OUT（越境封鎖だけ一律適用）。
2026-08-20 の検収者判断でも「本スライスで触らない」で合意している。

> `gotchas` は「踏んだ罠の記録」、ここは「未解決の課題」。役割が違うので両方に置く。

## cron の HTTP 応答を誰も読んでいない（2026-08-20 登録・**未解決**。2026-08-31 に射程を更新）

> **2026-08-31 追記（スライスCD）— この穴は埋まっていない。**
>
> ディスパッチャ（`dispatch-daily` / `dispatch-weekly`）を作ったことで**性質だけが変わった**。
> 「ディスパッチャを作ったので cron の穴は埋まった」と読まないこと。
>
> |                    | 内容                                                                                                                                                    |
> | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
> | **変わったこと**   | `deliver-*` / `run-sense` の失敗は**ディスパッチャが `await` して掴む**ようになった。全社の結果を集計し、1件でも失敗があれば non-2xx を返す（CD-2-2）   |
> | **変わらないこと** | **cron → ディスパッチャ**の呼び出しは依然 `net.http_post` の投げっぱなしである。ディスパッチャが 401 や 500 を返しても、**cron は成功のまま回り続ける** |
> | **唯一の検知**     | **毎朝届くはずのパルスが届かないこと。** これは仕組みではなく**人間の気づき**である                                                                     |
>
> つまり検知点が「誰も見ていない HTTP 応答」から「人間が気づくかどうか」に移っただけで、
> **機械的な検知は依然として無い。**

`00020` の pg_cron ジョブは `SELECT net.http_post(...)` で Edge Function を呼ぶ。
**`net.http_post` は非同期で、リクエストをキューに入れて即座に `request_id` を返す。**
HTTP応答は誰も待たず、誰も読んでいない。

**帰結: 関数が 401 / 500 を返しても、cron 側は成功のまま毎日回り続ける。**
`sync-connections` が止まっていても、止まっていることが**どこにも現れない**。
S-2-9 で `resolveCaller` を17本すべてに入れたことで、
「Vault の `sentio_service_role_key` が現行キーと食い違えば毎日 401」という
現実的な故障モードが増えており、この穴の重さは上がっている。

### 検証手順の側も同じ穴を踏んだ（2026-08-20）

**この不具合は、それを検証するはずの手順書にも入り込んでいた。**
スライスS の契約書には merge 前の停止点として
「cron の実行ログ（`cron.job_run_details`）で 401 が出ていないことを実測する」と書いていたが、
**`job_run_details` は `SELECT net.http_post(...)` が成功したことしか示さない。**
401 でも緑になる。検収者の指摘で判明し、`net._http_response` と
ダッシュボードの Invocations / Logs に差し替えた
（手順: `docs/runbooks/2026-08-20_delivery-idempotency.md` §3-2）。
同種の記述が `docs/secrets-runbook.md` の鍵ローテーション手順にもあり、
**「cron が succeeded だから旧シークレットを削除してよい」**という
最も危険な形で残っていたので同時に直した。

**仕組みで検知していない以上、人間の手順書が唯一の防波堤になっている。**
これがこの項目の重さそのものである。手順書は読まれないことがある。

### 判断が要ること

1. **cron 側で応答を読むか、関数側から結果を残すか。**
   前者は `net.http_post` の `request_id` を保存して後続ジョブで `net._http_response` を
   突き合わせる形になる。後者は関数が自分の実行結果をテーブルに書く形
   （`sync-connections` は既に `results` を返しているが、**返すだけで保存していない**）
2. **`net._http_response` の保持期間が短い**（既定で数時間）。
   突き合わせジョブを作るなら、刈られる前に走らせる必要がある
3. **失敗をどう知らせるか。** 記録するだけでは「誰も見ない記録」が1つ増えるだけになる。
   通知の宛先と閾値をセットで決めること

**着手前に必要なもの:** 401 を返す状態を作って、その失敗が実際に検知される
統合テスト。現状は「手順書に書いた」以上の担保が無い。

## フルハーネス日次上限 = 10 は暫定値（2026-08-20 登録・スライスS S-6-5）

`supabase/functions/_shared/budget.ts` の `MAX_FULL_RUNS_PER_DAY = 10` は
**暫定値であり、プラン設計時に再決定する。**

**10 になった経緯（契約 S-6-5）:** 3回だと合成会社の一気通貫（S-3-2）が上限に当たり、
テスト側で上限を上書きする経路が要る。それは「本番コードに `if (testMode)` を作らない」原則と
衝突する。10 なら合成会社を素通しできる、という**テスト都合で決まった数字**であって、
調査予算としての妥当性から導いた数字ではない。

**`light_runs` は上限なし・記録のみ**（`spec/03:52` の「超過はライトパス降格」を成立させるため。
ライトを絞ると降格先が無くなる）。

**再決定の条件:** 調査予算はプランの課金軸そのもの（本ファイル冒頭「事業判断待ち」の
プラン具体値）なので、**プラン別可変化はスライス5**。それまでは定数1つのまま動かさない。
環境変数化もしない（S-6-5）。

## ~~レガシー JWT キーの廃止と、ゲートウェイ JWT 検証の依存~~ → **クローズ（2026-08-29・ADR-0002）**

**決定済み。この項目はもう判断待ちではない。正本は `docs/adr/0002-auth-boundary.md`。**

- **決定**: 認証境界は `resolveCaller`（`supabase/functions/_shared/caller.ts`）の**一層**とする。
  追加の層は置かない。保証は `check:caller-guard`（デプロイ対象の全 Function が
  `resolveCaller` を通ることを CI で機械検査する）
- **期限は消滅した。** レガシー JWT キーからの移行は**すでに完了している**
  （Edge Function の env / Vault `sentio_service_role_key` / GitHub Secrets の3箇所とも
  新形式 `sb_secret_...`）。2026年末の廃止予定に追われる理由は無くなった

**この項目が立っていた前提は、4つとも実測で崩れた。**
崩れた前提の全文（何がいつどう崩れたか）は **ADR-0002 の「背景」に移した**。
判断材料は `docs/reports/2026-08-29_認証境界は一層である_判断材料.md`、
鍵形式の実測は `docs/reports/2026-08-27_service_role_key形式の実測.md`。要点だけ残す:

- **ゲートウェイは JWT を検証していない**（2026-08-27 本番実測）。
  「ゲートウェイ検証に依存する設計」という前提が事実でなかった
- 本番の service_role キーは**新形式**であり、レガシー形式ではなかった
- したがって「鍵の移行が認証境界を1層まるごと外す」は起きない。**外れる層が最初から無かった**
- 案D（`config.toml` に `verify_jwt = true`）は**採れない**。新形式キーでは JWT 検証が動かず、
  有効化すると内部呼び出し（cron・`invoke-function`・Day0）が全部止まる

**再決定の条件（ADR-0002 と同じ）:** Supabase が新形式キーでもゲートウェイの JWT 検証を
提供したら見直す。それまでこの項目は開かない。

## アカウント削除の約束を守り続けられるか（**API 化は約束ではなく選択肢**・2026-08-20 登録 → 2026-09-03 題を直した）

> **2026-09-03 改題。** 旧題は「アカウント削除**API**の実装」で、本文も
> 「『やるかどうか』ではなく『いつ・どう作るか』の問題である」と書いていた。
> **これは誤りだった。約束しているのは API ではない。**
> 題が「API の実装」だと、公開済みの約束そのものに見える。実際には
> **メールでの受付と30日以内の削除（手作業）**を約束しており、
> 手順書があり実行できる以上、**約束は現に守れている。**
> API 化は守り方の選択肢の1つであって、未着手でも違反ではない。

**根拠の出所は `src/app/privacy/page.tsx` のプライバシーポリシー §6 である。**
**利用規約ではない**（`src/app/terms/page.tsx` には削除も解約も1条も無く、
§7「サービスの変更・終了」は**提供者側が終了する場合**の条文である。2026-09-03 実測）。

§6「アカウントを削除する場合」の原文:

> アカウントの削除は support@mdc-diseno.com へのご連絡で承ります。ご本人であることを
> 確認のうえ、**ご依頼から30日以内に**、当該アカウントに紐づくすべてのデータ
> （Google ユーザーデータ、認証情報、生成された分析結果を含む）を削除します。
> 削除の完了はメールでご報告します。

**約束しているのは3つ**である。**受付の窓口（メール）／本人確認／30日以内の削除と完了報告。**
**画面から自分で消せること（API）は、どこにも書いていない。**

**当面は手作業で守る。** 手順は `docs/runbooks/2026-08-20_account-deletion.md`。
件数が少ないうちは、誤削除の危険を考えると自動化を急がない方がよい。

**なぜ課題なのか。** 手作業は3つの形で壊れる。

1. **担当者が1人しかいない。** 不在時に30日を超える
2. **本人確認が属人的。** 判断の基準が文書にしか無く、実行と検収が同じ人になる
3. **手順書と実装がずれる。** テーブルが増えたときに手順書の DELETE 列挙へ
   追記し忘れると、**消し残しが静かに残る**（`00013` のRLS検証リストへの追記漏れと同型）

3 が一番危ない。**「どのテーブルに company_id があるか」を機械で列挙する検査**があれば、
手作業のままでも消し残しを止められる。実装より先にそれを作る手もある。

**未判断の点（勝手に確定させない）:**

- 画面から自分で消せるようにするか、`support@` 経由の申請のままにするか。
  自分で消せる形は、誤操作と第三者による乗っ取り時の破壊が増える
- 削除を**即時**にするか、**猶予期間**（例: 7日間の取り消し可能期間）を置くか。
  ポリシーは「30日以内」としか書いていないので、どちらでも約束は守れる
- `auth.users` の削除を Sentio 側から行うか、Supabase の管理API に委ねるか

**着手前に必要なもの:** `company_id` を持つテーブルの機械的な列挙と、
それが手順書・実装の DELETE 対象と一致していることの検査
（`check:ci-coverage` と同じ「宣言と実物の突合」の形）。
これが無いまま自動化すると、消し残しがコードの中に固定される。

### 2026-09-03 — 予告した「3」が現物として出た。検査器を作った

**上の3（手順書と実装がずれる）が、予告のまま起きていた。**

```
company_id を持つテーブル: 11
手順書の DELETE 対象     : 10
消し残し（DELETE されない）: ['known_explanations']
```

`known_explanations`（`00008_create_known_explanations.sql:6` に `company_id UUID`）が
手順書の DELETE 列挙から漏れていた。ポリシー §6 は「当該アカウントに紐づく**すべての**
データを削除します」と公開済みなので、**このまま実行すれば約束違反になっていた。**

対応は3つ。

1. **原則を置いた。** `company_id` を持つテーブルは**全件が DELETE の既定対象**である。
   残すものは `docs/checklists/deletion-coverage.yml` の `keep` に
   **「残す理由」を書いて**例外に挙げる。「消す理由」は書かない
2. 手順書の列挙に `known_explanations` を足した（**足しただけ。他の列挙は変えていない**）
3. **恒久検査器 `check:deletion-coverage` を作り、`ci.integration` に載せた。**
   実DBの `information_schema` から `company_id` を持つテーブルを引き、
   手順書の DELETE 列挙と両方向で突き合わせる

**守れない範囲は宣言ファイルの冒頭に書いた。** `company_id` を持たない経路で会社に
紐づくデータ（中間テーブル経由、`auth.users.user_metadata`、Vault の秘密、Storage、
Stripe / Resend に残る記録）は**見ない。** 射程が違うのであって不具合ではない。
もう1つ、**「手順書に書いてあるか」しか見ない。** 人が手順どおり実行したかは見ていない。

**`keep` は現時点で0件である。** 何を残すか（保存義務のある記録の範囲）は
**未判断**なので、決まるまで例外を置かない。置かない方が、
消し残しではなく「消しすぎ」の側に倒れるためである。

## 取り消しから30日経ったデータを消す処理が無い（D-3 未実装・2026-08-21 登録 → 2026-09-03 射程を縮めた）

> **2026-09-03 全面改稿。** 登録時の記述（「API はあるが呼ぶ者がいない」「revoke と
> 一時的失敗を区別する材料が現状は無い」）は、**契約D（`slice-disconnect.md`・2026-08-27）で
> 解決済みであり、事実ではなくなっていた。** 実装済みの2経路を未実装として書き続けると、
> 次に読んだ人が「ポリシー §6 は丸ごと守れていない」と誤読する。実態に合わせて書き直す。

プライバシーポリシー §6（`src/app/privacy/page.tsx`）は**2つの経路**を並記している。

> お客様が **Sentio の画面** または **Google アカウントの設定** から Google との連携を
> 解除した場合、アクセストークン・リフレッシュトークンを直ちに破棄します／
> 当該連携から取得した Google ユーザーデータを30日以内に削除します。

### 実装済み（契約D で埋まった）

| 経路                             | 実体                                                                                                                                                                                                                                             |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **1. Sentio の画面**             | `src/app/connect/connect-client.tsx` の二段確認（ログイン中のメールアドレス入力）→ `src/app/api/connections/disconnect/route.ts` が Vault の秘密を破棄し、当該 provider 由来の `events` を削除し、`connections` の行を削除する                   |
| **2. Google 側の取り消しの判別** | `supabase/functions/_shared/token-refresh.ts` の `classifyTokenFailure` が、**HTTP 400 かつ `error === "invalid_grant"` のときだけ** `revoked` に倒す。それ以外は `reauth_required`（fail-safe 側）。通信断や一時的失敗で `revoked` にはならない |

30日の起点は **`connections.revoked_at`**（migration `00027`）。判別が付いた時刻がここに入る。

### 残っているのは1つだけ — **D-3（30日経過後の削除）**

**`revoked_at` から30日経ったデータを実際に消す処理が無い。**

つまり、Google 側で取り消されたことは**検知でき、記録もされる**が、
そこから30日後に `events` を消す動作が存在しない。
ポリシー §6 の「30日以内に削除します」のうち、**経路2の削除だけが未実装**である
（経路1は解除の操作そのもので即座に消えるので、この穴に入らない）。

受入基準は契約D に既に書いてある（D-3-1 〜 D-3-5）。
`retention-purge` に載せる形で、`src/lib/retention/policy.ts` を30日の正本とし、
`MAX_DELETE_ROWS` 超過時は**消さずに止める**——設計は決まっている。**実装だけが無い。**

### 着手条件（**契約D の停止点。勝手に外さない**）

> **2 と 3 の間に本番実績を見る停止点を置く。** `revoked` が立った実例を1件以上観測し、
> それが本当に取り消しだったかを人間が確認するまで、D-3 を有効にしない。
> 検知が壊れたまま自動削除を入れると、**気づいたときには消えている。**
> （`docs/contracts/slice-disconnect.md` 実装順）

**本番で `revoked` の実例を1件以上観測し、人間が「本当に取り消しだった」と確認するまで、
D-3 に着手しない。** `invalid_grant` は取り消し以外（トークン期限切れ・6ヶ月無操作・
パスワード変更）でも返るため、判別の精度は実データでしか確かめられない（D-3 の判断根拠）。

**この条件は 2026-09-03 に満たされた。** 下の §7 の実測と**同じ機会**である。
本番で2件が `revoked` に倒れ、**人間（梶谷さん）が自分で取り消したものであると確認済み**。
`invalid_grant` の判別は正しく働いた。**D-3 は着手可能になった**（実装は別 PR）。

**着手する側への注意。** いま本番にある `revoked` の2行は `revoked_at` が
**2026-09-03 06:00 UTC** である。D-3 が入ると **2026-10-03 に、この2社の
Google カレンダー由来のイベントが自動で削除される。** これは検証用に作った状態であって、
**消えて困るデータ**（会社1・会社2 の予定22日ぶん、`schedule_interval` ベースラインの土台）
である。再連携すれば `revoked_at` は `NULL` に戻り、削除の対象から外れる（契約 D-2-6）。

## ~~ポリシー §7 の「最大1時間程度」と、cron の6時間おきが食い違っている~~ → **(a) で確定（2026-09-03・実測 4時間46分）**

> **2026-09-03 に同日で閉じた。** 登録時は「実測を見てから決める」としていたが、
> その実測が同日に取れた（下記）。**梶谷さんの判断で (a)＝文言を実態に直す。**
> 以下の事実の記述は登録時のまま残す。**何を見て決めたかが分からなくなるため。**

プライバシーポリシー §7（`src/app/privacy/page.tsx:202`）は、こう公開している。

> 取り消しが画面の表示に反映されるまで、**最大1時間程度**かかることがあります。

**実態は最大6時間である。**

取り消しの判別（`classifyTokenFailure`）はトークン更新の失敗時にしか起きず、
その更新を回しているのは `sync-connections` の cron だけである。

```sql
-- supabase/migrations/00020_*.sql
PERFORM cron.schedule('sync-connections', '0 0,6,12,18 * * *', v_command);
```

**6時間おき**（UTC 00:00 / 06:00 / 12:00 / 18:00）なので、取り消した直後に cron が
走った直後だった場合、画面に反映されるまで**最大6時間**待つことになる。
「最大1時間程度」は満たせない。

### 選択肢（登録時に並べたもの）

- **(a) 文言を実態に直す。** §7 を「最大6時間程度」にする。コードは変えない。
  ただし **URL が変わらないので OAuth の再審査は引かない**
  （`.claude/rules/oauth-consent-screen.md`「引き金にならないもの」の第1項
  「**ポリシー本文の更新。** URL が変わらなければ再審査に戻らない」。
  **この規則は PR #83 で main に入った。** 登録時（2026-09-03 午前）は未merge の
  ブランチにしか無く、`.claude/rules/` を見ても存在しない状態だった）
- **(b) cron の間隔を縮める＋401 での判定を足す。** **cron だけでは足りない**（下記）。
  **API の呼び出し回数が6倍になる。** Google のクォータと、
  KING OF TIME の接続禁止時間帯（JST 8:30–10:00 / 17:30–18:30）の制約が絡む

### (b) は単独では成立しない（2026-09-03 追記）

**cron を縮めても、それだけでは §7 の文言を守れない。** 検知の頻度を決めているのは
cron の間隔ではなく、**アクセストークンの寿命**だからである。

取り消しの検知が起きる経路は1本しかない。

```ts
// supabase/functions/sync-connections/index.ts:73-74
if (isTokenExpired(conn.expires_at)) {
  const refreshResult = await refreshToken(conn, supabase, (k) => Deno.env.get(k));
```

**`revoked` を書くのは `classifyTokenFailure` だけ**である（`_shared/token-refresh.ts:71`。
リポジトリ全体で1箇所。判定を使う書き込みも同ファイルの `:166` / `:263` のみ）。
つまり**リフレッシュを試みなければ、取り消しは永久に分からない。**

Google のアクセストークンは**約1時間**で切れる。`isTokenExpired` は期限の5分前から
true を返す（`EXPIRY_BUFFER_MS = 5 * 60 * 1000`）。したがって:

| cron の間隔   | 各回の挙動                                                  | 検知の遅れ                |
| ------------- | ----------------------------------------------------------- | ------------------------- |
| 6時間（現状） | 毎回トークンが切れている → **必ず** refresh に入る          | 最大6時間                 |
| 1時間         | ほぼ毎回 refresh に入る                                     | 最大1時間強               |
| **1時間未満** | **多くの回でトークンが期限内** → **refresh 分岐に入らない** | **1時間より短くならない** |

**間隔を1時間より短くしても検知は速くならない。** トークンが有効な回は
`else` 側（Vault から読み出すだけ）に落ち、判別の機会そのものが無い。
API 呼び出しだけが増える。

### 守るには、もう1つ実装が要る

**「最大1時間程度」を守るには、データ取得の API が 401 を返したときも `revoked` と
判定する実装が別途必要である。** リフレッシュの失敗を待たず、取得の失敗から気づく経路になる。

**現状、`401` を扱う箇所はリポジトリに1つも無い**（`sync-connections/index.ts` /
`_shared/token-refresh.ts` を grep 済み。API 呼び出しの失敗は `status: "error"` に
丸められ、種別を見ていない）。

**上の (b) の見積り「migration 1本」は、これを含んでいない。**
(b) を選ぶなら、実際に要るのは次の2つである。

- **(b-1)** cron の間隔を縮める（migration 1本）
- **(b-2)** 401 を `revoked` と判定する経路を足す（`classifyTokenFailure` の対に当たる実装）

**(b-2) は契約D の禁止事項に触れる設計判断を含む。**
「`invalid_grant` 以外を `revoked` に丸めない。迷ったら `reauth_required`」が原則であり、
401 は取り消し以外（スコープ変更・一時的な認可エラー）でも返りうる。
**どの 401 なら取り消しと断定してよいかを決めないと、誤判定が
D-3（30日削除）の入力になる。** 上の「取り消しから30日経った〜」と同じ危険である。

### 実測（2026-09-03・本番 `connections`）

梶谷さんが Google アカウント設定から連携を取り消した。**意図的な実験である。**

```
provider          status    revoked_at                  last_refresh                expires_at
google_calendar   revoked   2026-09-03 06:00:03.841+00  2026-09-03 00:00:03.929+00  01:00:02+00
google_calendar   revoked   2026-09-03 06:00:04.006+00  2026-09-03 00:00:05.370+00  01:00:03+00
```

```
取り消し  2026-09-03 01:14 UTC（10:14 JST）
検知      2026-09-03 06:00:03 UTC（15:00 JST）
遅延      4時間46分
```

**「最大1時間程度」は実測で破れた。** 4時間46分は6時間おきの cron から出る値の範囲内であり、
**上の見立て（実態は最大6時間）が実データで裏づけられた。**
`expires_at` は `01:00` で取り消しの14分前に既に期限切れだったので、15:00 の回で確実に
refresh に入る状態だった。**遅延の理由は cron の間隔だけである**——(b-2) の 401 経路が
無いことは、この回の遅延には効いていない。

### 決定（2026-09-03 梶谷さん）— **(a) で確定**

**(b) を選ぶ理由が §7 の中に無い。** 取り消しの後は次の cron まで取り込みも起きないので、
**表示が古いこと以外の実害が無い。** D-3 の30日の起点（`revoked_at`）が最大6時間ずれるのは
誤差である。

やったこと。

- `src/app/privacy/page.tsx:202` の「最大1時間程度」→ **「最大6時間程度」**
- 同ページの `UPDATED_AT` を **2026-09-03** に更新
- **cron（migration `00020`）は触らない。401 を `revoked` と判定する経路 (b-2) も作らない**

**OAuth の再審査は引かない。** `.claude/rules/oauth-consent-screen.md`
（**PR #83 で main に入った。**判断した時点では未merge のブランチにしか無かった）の
「引き金にならないもの」第1項:

> **ポリシー本文の更新。** URL が変わらなければ再審査に戻らない
> （§6 の文言を直す、条項を足す、などは自由）

引き金は「スコープ」「アプリ名 / ロゴ / サポートメール / 承認済みドメイン」
「プライバシーポリシー・利用規約の **URL**」の3つで、いずれも変えていない。
`/privacy` の URL は同じである。

### 閉じていない部分 —— 下の「cron を6時間おきのままにするか」に移した

**(b) を捨てたのではない。** 「§7 の文言を守るために cron を縮める」理由が無くなっただけで、
**取り込みの鮮度という別の理由は残っている。** 別項目として下に登録した。

## cron を6時間おきのままにするか（取り込みの鮮度・未判断・2026-09-03 登録）

**上の §7 とは別の理由で、同じ cron が論点になる。** §7 は (a) で閉じたので、
**ポリシーの都合で cron を縮める理由はもう無い。** 残っているのは製品の側の理由である。

`sync-connections` は6時間おき（UTC 00:00 / 06:00 / 12:00 / 18:00）なので、
**朝 07:00 JST に届くパルスが見ているデータは最大4時間前のものになる**
（直前の同期が 18:00 UTC = 03:00 JST）。

**これはポリシーとは無関係の、製品の鮮度の判断である。** 「昨日の夕方に起きたことが
今朝のパルスに入っているか」という問いであり、守れていない約束の話ではない。

### いま決めない理由

**単独で cron を触らない。** 間隔を縮めれば API 呼び出しは6倍になり、Google のクォータと
KING OF TIME の接続禁止時間帯（JST 8:30–10:00 / 17:30–18:30）の制約が絡む。
**鮮度がいくら必要かは、鮮度を使う側が決まっていないと決められない。**

**着手の合図: 「毎朝のメールに読む理由を作る」の設計時。**
そこで鮮度が要ると判断されたときに、必要な間隔と一緒に決める。

## `state-narratives` の呼び出し元をどこに置くか（2026-08-24 登録・**いま決めない**）

契約 `slice-state-repair.md` の **S-3-4** で構成は確定した。
夜間の記銘経路は `state-baselines` と `state-summary` の**2本**であり、
`state-narratives` は**イベント駆動の単発API**で夜間 cron には載せない
（契約 P-2 / `supabase/functions/state-narratives/index.ts:3-7`）。
仕様 `02_state.md` の記銘3経路との整合は
`docs/reports/2026-08-24_S-3-4_記銘経路の整理.md` に示した。

**残っているのは「(b) narratives upsert を誰が呼ぶか」だけである。**

**現状（2026-08-24 実測）:**

```
$ grep -rn '"dialogue"' supabase/functions/ src/ --include="*.ts" | grep -v test
（0件）
$ grep -rn "state-narratives" supabase/functions/ src/ --include="*.ts"
（state-narratives/index.ts の自己言及のみ。本番コードからの呼び出しは0件）
```

**いま決めない理由:**

- 発生源である「Sentioに聞く」が未実装で、契約 `docs/contracts/slice-ask.md` 自体が保留中である
- **呼び出し元だけ先に作ると、誰も呼ばないコードが1本増えるだけになる。**
  disconnect API が2026-08-27 まで置かれていたのと同じ形（実装はあるが経路が繋がっていない）を
  もう1つ作ることになる（**あちらは契約D で繋がった。**上記「取り消しから30日経った〜」を参照）
- 記銘専用の Function を立てる案は**デプロイ対象を19本目にする**。
  CI のプローブ対象と `deploy.yml` の突合（S-3 で追加）の期待値も変わる。
  いま19本目を作る理由が無い

**決め方:** `slice-ask` の契約で、発生源の設計と**一緒に**決める。
そのとき候補になるのは次の2つ。`run-sense` の中に混ぜる案は契約 P-2 が既に排除している。

1. **ingest 経路から呼ぶ** — `event_type: "dialogue"` を受けた時点で呼ぶ。
   契約 S-3-4 の文言に忠実だが、ingest（取り込み層）が State 層を呼ぶ向きの依存が新しく生まれる
2. **記銘専用の呼び出し元を1本立てる** — ingest はイベントを流すだけにし、
   「何を記銘するか」の判断をそこに閉じる。依存の向きは綺麗だが Function が1本増える

## カレンダーの未来の予定を取り込むか（未判断・2026-08-31 登録）

`sync-connections`（`supabase/functions/sync-connections/index.ts`）と初回連携
（`src/app/auth/callback/google/route.ts`）は**どちらも `timeMax = now`** で、
**未来の予定を一切取り込まない。**

`/report` はスライスRF のフォールバック（当週が0件なら直近の実績週へ最大8週遡る）で
当面しのぐが、**「今週これから何があるか」を見せる機能を作るなら `timeMax` の判断が要る。**

### 絡む論点は3つある

1. **S2 の allowlist。** 未来の予定を入れると `events` の意味が
   「起きたこと」から「起きること」を含むものに変わる。
   既存の集計（`summarizeWeek` は `period_start` が週内の行を数える）は
   過去・未来を区別していないので、**取り込んだ瞬間に当週の件数の意味が変わる**
2. **イベント量。** 現在は過去7日ぶんを毎回同期している。
   未来を含めると同期量と保持量の見積もりが変わる
3. **「まだ起きていないこと」を Finding の材料にしてよいか。**
   Scanner の `deadline` / `silence` は「起きなかったこと」を見る。
   予定という「これから起きるはずのこと」を同じ土俵に載せるかは製品判断である

**人間の判断待ち。勝手に確定させない。**
スライスRF はこの判断を**避けて**通しており（RF-D7）、`timeMax` は触っていない。

## Scanner の `scanType` ラベリングが仕込みとずれている（2026-08-29 登録・スライスE の実測由来）

**Scanner は仕込み7件すべてのイベントに触れている。見えていないのではなく、
候補に付ける `scanType` が仕込みの型とずれている。**

出所は `docs/contracts/slice-eval-repair.md`（スライスE）の実測。
評価スイートが証拠まで見るようになって初めて顕在化した。

### 実測（2026-08-29・現行 Scanner）

| 指標      | 直す前の採点 | 証拠まで見た採点 | 合格線 | 判定     |
| --------- | ------------ | ---------------- | ------ | -------- |
| D1 検知   | 7/7          | **5/7**          | 6以上  | **未達** |
| D2 誤検知 | 0件          | **1件**          | 2以下  | 達成     |

**`scanType` を見ずに `evidence_event_ids` の交差だけで数えると 7/7 になる。**
落ちている2件は、どちらも `trend` として仕込んだものである。

| signal                        | 実際に起きていること                                                                                                                                                                   |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ① `order_interval_elongation` | 仕込みイベントを参照する候補は**すべて `deviation`**で、内容も `Revenue 50000 outside range [70500, 130500]`。**発注間隔の伸長としては見ていない**（金額の外れ値として拾っているだけ） |
| ④ `overtime_creep`            | 候補の説明は `Overtime hours increasing: 1 → 3 (5 consecutive points)` で、**現象は正しく捉えている**。ただし `scanType` が `trend` ではなく **`deviation`** と名乗っている            |

**④は「見つけているが名前が違う」、①は「別のものとして見ている」。同じ修正では直らない。**

あわせて、**唯一の `trend` 候補が正常データ（`txn_normal_*`）に出ている**（D2 の誤検知1件）。
`trend` の判定そのものが、当たるべきところに当たっていない。

### なぜ今すぐ直さないか

- スライスE は**測り方だけ**を直す契約であり、Scanner の変更は非スコープ（E-D5）
- **測った直後に測られる側を直すと、修復の効果と Scanner の変更が混ざって切り分けられない**
- `scanType` は Finding の分類・緊急度・配信経路に効く。**採点を通すために付け替えるのは順序が逆**

### 判断が要ること（**勝手に確定させない**）

1. **④を `trend` と名乗らせるのか、仕込み側の期待を `deviation` に寄せるのか。**
   現象を正しく捉えているのはどちらの名前が正しいかとは別の問題である
2. **①を発注間隔の伸長として見る `trend` 検知を足すのか。**
   いまは金額の外れ値としてしか見ていない。足すなら新しい検知であって、ラベルの付け替えではない
3. **`trend` が正常データに当たっている件**（D2 の誤検知1件）を同時に直すか、分けるか

### 着手の合図

D1 の期待値は `tests/eval/engine.test.ts` で **実測値 5 に固定してある**
（`toBe(5)`。上振れも回帰も赤にして現在地の更新を強制する形）。
**この行が赤くなったら、Scanner の挙動が変わったということである。**
そのときは期待値を直すのではなく、**なぜ変わったかを確かめてから現在地を更新する。**

## 全銀協フォーマットの取り込み（未判断・2026-08-31 登録）

銀行が書き出す入出金明細には**列名の行が無い形式がある**（実物を確認済み。
`docs/reports/2026-08-31_CSV_ヘッダー行前提が崩れる.md`）。

スライスCH（`docs/contracts/slice-csv-headerguard.md`）では
**検出して断るだけ**にした（CH-D1・案(b)）。取り込むなら**レコード区分ベースのパーサ**が要り、
そのとき**列推定（LLM）は不要になる**——レコードの並びが仕様で決まっているからである。

### 判断が要ること（**勝手に確定させない**）

1. **対応する銀行の範囲。** 全銀協規定フォーマットは銀行ごとに細部が違う。
   1行の実物で1行だけ通す形にすると、次の銀行でまた同じ事故が起きる
2. **半角カナと Shift-JIS の扱い。** この形式は半角カナが前提であり、
   文字コードの判定を外すと**名義が化けたまま取り込まれる**。
   いまの `connect-client.tsx` は Shift-JIS を試して失敗したら UTF-8 に落とすだけで、
   **化けたかどうかは見ていない**

**この2つはセットで決める必要がある。** 範囲だけ決めても文字コードで崩れる。

## API ルートの認証が宣言と揃っていない（**2026-09-02 解決**・2026-08-31 登録）

> **2026-09-02 追記。ずれていた2件はどちらも片付いた。判断待ちではない。**
>
> - **`/api/analyze-url` は削除した**（PR #62）。呼び出し元が1つも無く、
>   Day0 の「外から見た自社」は Edge 側の自前実装で動くので機能は1つも失っていない
> - **`/api/csv/analyze` の認証は足していない。** スライスCH がサーバ側にも判定を置いたため、
>   列名の流出という意味では閉じている。残るのは費用と濫用で、**それは別の判断である**
>   （認証境界は `docs/adr/0002` で確定済みであり、ADR の更新とセットでなければ動かせない）
> - `/api/competitors/suggest` は認証があり、**呼び出し元も作った**（PR #77）
>
> **`/api/csv/analyze` に認証を課すかだけが残っている。** 以下は登録時の記録である。

**`src/middleware.ts` は `/api` を matcher から明示的に外している。** 理由はコメントに書いてある。

> api / auth も外す。これらは `getAuthedContext()` で自前に認証しており、
> middleware でも `getUser()` を呼ぶと1リクエストで2回セッションを解決し…

**つまり「API ルートは自前で認証する」がこのリポジトリの宣言である。** 実態を数えた。

| route                                                                                                          | 自前認証                  | 評価                                          |
| -------------------------------------------------------------------------------------------------------------- | ------------------------- | --------------------------------------------- |
| `auth/google` / `auth/freee` / `connections` / `connections/disconnect` / `competitors/suggest` / `csv/ingest` | `getAuthedContext()` あり | 宣言どおり                                    |
| `auth/session` / `auth/signout`                                                                                | 無し                      | **正しい**。セッションを作る/壊す入口そのもの |
| `csv/analyze`                                                                                                  | **無し**                  | 宣言とずれている（下記1）                     |
| `analyze-url`                                                                                                  | **無し**                  | 宣言とずれている（下記2）                     |

### 1. `/api/csv/analyze` に認証が無い

契約 スライスCH は「API は直接叩ける」を前提に**サーバ側にも判定を置いた**ので、
列名の流出という意味では閉じている。**残るのは費用と濫用である。**
1回あたり Anthropic の呼び出しが走り、実測で 30 秒かかっている（復旧実測 2026-08-31）。

**判断が要ること。** `getAuthedContext()` を足すか、意図的に公開のままにするか。
足す場合、スライスCH の受入基準（CH-1-3 / CH-1-4 / CH-3-3）は
**未認証のまま POST して検証している**ので、試験側の作り直しが要る。
認証境界は `docs/adr/0002` で「resolveCaller の一層」と確定済みであり、
**ADR の更新とセットでないと決められない。**

### 2. `/api/analyze-url` は認証も呼び出し元も無い

`GET /api/analyze-url?url=…` は**任意のURLをサーバ側で取得し**、
`title` / `description` / `og*` / `lang` を返す。**到達先の制限が無い**（SSRF の形）。

さらに**このルートを呼んでいるコードがリポジトリに1つも無い。**
`supabase/functions/day0/index.ts` は同名の `analyzeUrl` を**自前に持っており**、
この API を経由していない。**D-4-1（APIはあるが誰も呼ばない）の形そのものである。**

`check:endpoint-callers` は緑のままである。宣言（`ENDPOINT_SPECS`）に
`disconnect` の1件しか載っていないためで、**検査器の穴ではなく宣言の穴**である。

実測（2026-08-31・プレビュー。Vercel の保護は回避したが**Sentio のセッションは持っていない**）:

```
GET /api/analyze-url?url=https://example.com
→ 200 {"url":"https://example.com","title":"Example Domain",...}
```

**判断が要ること。** 3つあり、どれを採るかで作業がまるで違う。

1. **消す**（誰も呼んでいないので、消せば面がまるごと無くなる。いちばん確実）
2. **認証を足す**（将来使う予定があるなら）
3. **到達先の許可リストを付ける**（外部サイトの解析が用途なら、認証だけでは足りない）

**勝手に消さない。** 使う予定があるかは人間しか知らない。

## 列名の行が2行目以降にあるCSV（未判断・2026-08-31 登録・スライスCH の残余）

スライスCH の判定は**1行目だけ**を見る（CH-D4）。
したがって**1行目がタイトル行、2行目が列名**という形は**検出できない**。
通れば `○○銀行 入出金明細` のような1行目がプロンプトに載る。

列数の食い違い（1行目だけセルが極端に少ない）で検出できる見込みはあるが、
**それは CH-D4 とは別の判定**であり、スライスCH の停止点
（「今回足すのは新しい1本の分岐だけ」）の外にある。

### 判断が要ること

1. **2行目を読むか。** 1行目との列数比較には2行目が要るが、**2行目はデータ＝PII である。**
   読むこと自体は Sentio のプロセス内で完結し、プロンプトには載らないが、
   「判定のためにどこまで読むか」は線を引いておく必要がある
2. **前置き行を飛ばして取り込むか、断るか。** 飛ばせば使えるようになるが、
   何行飛ばすかを機械が決めることになる。CH-D1 と同じ「対応せず断る」で揃えるのが素直

**この形の実物はまだ出ていない。** 出ていない段階で判定を足すと、
CH-2 系（誤検知を出さない）の側が先に壊れる。実物が出てから決めるのが順序である。

## 途絶（沈黙）のシリーズ単位 ／ 発注間隔の伸長（**2026-09-02 解決**）

2026-09-01 に「未判断」として登録した2件は、**同日中に梶谷さんの判断を得て実装した。**
記録として経緯を残す。**判断待ちではない。**

### 決まったこと

- **シリーズ別を作る**（判断1）。**発注間隔も同時にやる**（判断2）
- 実装は `supabase/functions/_shared/scan.ts` の走査7・8。**途絶と伸長は同じ間隔列から出る**ので、
  1つの仕掛けで両方が埋まった（PR #76）

### 見積もりを訂正した

登録時に「`entities` 行の生成が要る＝契約1本分」と書いたが、**過大だった。**
`baselines.entity_id` に外部キーが無いことを確認したので、
`entities` 行を作らずシリーズ名から導いた値で成立する。

### 併せて見つけた仕込みの誤り

`#1 order_interval_elongation` は**名前と逆の形**だった。時系列に並べると
間隔が 50 → 35 → 25 → 18 と縮み、売上は 30000 → 50000 と増えていた——
「取引先が離れていく」ではなく**「発注が増えて単価も上がる」＝良い兆候**のデータで、
golden の記述「発注間隔が3ヶ月かけて伸長」とも食い違っていた。

**これを直さずに検知を書くと、「間隔が縮んだら検知する」という名前と逆の実装になる。**
仕込みの側を直した。

### 残っている限界

シリーズの同定は**予定のタイトル・取引先の摘要**で行う。
**タイトルが変わると別シリーズになる**（「定例MTG」→「週次定例」）。
実データで問題になったら、そのとき同定の仕方を決める。

**D1 は 7/7 に到達した**（D2 誤検知 0件）。

## `/api/competitors/suggest` の呼び出し元（**2026-09-02 解決**）

2026-09-01 に「未判断」として登録した件は、**同日中に梶谷さんの判断を得て実装した。**

**「呼び出し元を作る」を選択**（消さない）。そのために自社サイトのURLが要るが、
アプリはどこにも持っていなかった（`companies` テーブルが存在せず、登録画面でも聞いていない）。

**登録時に自社サイトのURLだけを聞く**ことにした（判断基準「入力を要求していないか」に対して、
1項目に絞る線引き）。会社名も業種も聞かない。URL は `auth.users.user_metadata` に置き、
**新しいテーブルを作っていない**（PR #77）。

エンドポイントは**冪等**にした。呼び出し元は画面の読み込みごとに叩きうるので、
これが無いと**開くたびに Anthropic と gBizInfo を叩き、entities が重複して増える。**

`check:endpoint-callers` の宣言にも載せた（8件 → 9件）。

## ライトパス降格が実装されていない（未判断・2026-09-02 登録）

**仕様と実装が食い違っている。**

> `spec/03:52` 会社別調査予算（…スコア順消化、**超過はライトパス降格**）

実装は**降格していない。止めているだけ**である。

```ts
if (!canRunFullHarness(fullRuns)) {
  budgetStopped = true;
  console.warn(`… 上限に達したためフルハーネスを起動しない（残り N 群を見送り）`);
  break; // ← ライトパスに落とさず、抜けるだけ
}
```

裏付けは3つある。

1. **`light_runs` を増やしている箇所が1つも無い**（`{ light_runs: 0 }` の初期化だけ）
2. ライトパスの生成経路が存在しない（`spec/03:13` の「単発生成＋評価」に当たるコードが無い）
3. `budgetStopped` は**レスポンスに載るだけ**で、どこにも保存されていない

つまり上限に達すると、その日の残りの調査群は**簡易調査になるのではなく、消える。**

### これが効いてくる場面

2026-09-02 に「上限に当たったとき週次レポートに1行出す」と決めた。
しかし**「今週は◯件を簡易調査に切り替えました」は、いま書いたら嘘になる。**
正しくは「◯件の調査を見送りました」だが、**それも今は書けない**——
`stopped_by_budget` が永続化されておらず、週次の集計時点で引く先が無い。

### 判断が要ること

1. **ライトパスを実装するか、仕様の方を「見送り」に改めるか。**
   実装すれば `spec/03` の記述が本当になる。改めるなら、プランの説明も
   「簡易調査に切り替わる」ではなく「見送る」に変わる
2. **見送りを記録するか。** `budget_usage` に列を足せば週次に出せる。
   ただしマイグレーションが要る（S2 の allowlist と RLS もセット）

**課金のプランはこの挙動を前提に説明することになる**ので、価格を対外に出す前に決める必要がある。

## 解約の導線が無い（未判断・2026-09-03 登録・スライスBU の非スコープ）

**購読は始められるが、画面からは止められない。**

スライスBU で `/connect` に「プラン」の節を置き、`POST /api/billing/checkout` へ
到達できるようにした。**購読中の表示は「標準プラン・購読中」の1行だけ**で、
解約・プラン変更のボタンは置いていない（BU-D4）。

置かなかったのは、解約が**画面を1つ足す話ではない**からである。
Stripe カスタマーポータルのセッションを作るエンドポイント（`billingPortal.sessions.create`）と、
戻り先の扱いが要る。購読の開始（1本）と同じ手数では終わらない。

### いま利用者が解約するには

**Sentio の外で行うことになる。** 支払い時の Stripe のメール（領収書）から
顧客ポータルに入るか、問い合わせを受けて手作業で止めるかである。
**どちらも画面には書いていない。**

### 2026-09-03 — 「カスタマーポータルを既定とする」は撤回された

検収側から一度「Stripe のカスタマーポータルへ送る導線を既定とする」という指示が出たが、
**BU-D4 の実物を確かめずに書かれたものとして、同日中に撤回された。**

原文を当たった結果はこうである（`docs/contracts/slice-billing-ui.md:38`）。

> | **BU-D4** | 購読中の見せ方 | **「標準プラン・購読中」と出すだけ。** 解約の導線は**このスライスでは作らない**（Stripe カスタマーポータルの実装が要る。`07_open_items.md` に登録する） |

**「このスライスでは作らない」であって「恒久的に作らない」ではない。** 根拠は3つ。

1. 本文がスライスを明示して限定している
2. 続けて「`07_open_items.md` に登録する」と書いている。
   **恒久に作らないなら登録は要らない**
3. 同契約の「非スコープ（やらない）」の節には
   `DEFAULT_PLAN` を `TRIAL_PLAN` に倒すこと（**Stripe が回り始めてから**）が
   並んでおり、**明らかに後でやる項目を含む。**
   したがってこの節は「恒久禁止」ではなく**このスライスの範囲外**の一覧である

**そして BU-D4 は方式を決めていない。** 「カスタマーポータルの実装が要る」は
**手数が大きい理由**として書かれているのであって、ポータルを採ると決めた記述ではない。
`src/app/connect/connect-client.tsx:513-514` のコメントも同じ形である。

```tsx
// 解約の導線は**このスライスでは作らない**（BU-D4）。
// カスタマーポータルの実装が要るので `07_open_items.md` に登録してある
```

**方式は未判断のままである。** 下の1がそれである。

### 判断が要ること（**勝手に確定させない**）

1. **カスタマーポータルを使うか、自前で作るか。** ポータルなら Stripe 側の
   設定画面で解約・支払い方法変更の可否を決めることになり、**文言も Stripe のものになる**
2. **解約したあと、いつ枠が下がるか。** `lib/billing/plan.ts` は
   `canceled` を既定（いまは標準）に落としている。期末まで使えるのか、即座に下がるのかは、
   `DEFAULT_PLAN` を試用に倒す判断（`09_pricing.md` の非スコープ）と同時に効く
3. **特定商取引法に基づく表記の「解約条件」と揃うか。** 有償化の時点で掲示が要ることは
   このキューに登録済みで、**掲示する解約条件と実際の導線が食い違ってはいけない**

### 着手の合図

**最初の1社が購読したとき。** 購読者が居ない間は、止める導線が無くても誰も困らない。
逆に1社でも入ったら、**止められないことは約束の問題になる。**

## `trialing` を購読中と見なすか（未判断・2026-09-03 登録・スライスBU）

**いまは `active` だけを購読中として扱っている**（契約 BU-1-2 のとおり）。
Stripe が返す `trialing` は、画面では**試用中の側に落ちる**——
つまり**購読ボタンが出たままになる。**

```ts
// src/app/connect/connect-client.tsx
const subscribed = subscriptionStatus === "active";
```

枠を与える判定（`src/lib/billing/plan.ts` の `ENTITLED_STATUSES`）は
`active` と `trialing` の両方を通す。**画面と枠で見ている集合が違う。**

### いま実害が無い理由

**Stripe 側の trial を使っていない。** `09_pricing.md` の「まだ決めていないこと 3」で
試用の期間そのものが未決であり、Sentio の試用は「購読が無い状態」＝0円/5回として
表しているので、`trialing` は現状どこからも書き込まれない。

### 判断が要ること（**勝手に確定させない**）

1. **Stripe の trial を使うか。** 使うと決めた日に、この行が**二重購読の入口になる。**
   trial 中の会社に「標準プランにする」を見せることになり、押せば2本目の購読ができる
2. **画面と枠で集合を揃えるか。** 揃えるなら判定を1か所（`plan.ts` 側）に寄せる。
   揃えないなら、**なぜ違うのかを両方に書く**

### 着手の合図

**`09_pricing.md` の「試用の期間」を決めるとき。** trial を Stripe 側に持たせる判断を
した時点で、この項目は未判断ではなく**バグ**に変わる。

## `?billing=done` で戻った直後、まだ「試用中」に見える（**外部顧客に出す前の必須項目**・2026-09-03 登録）

**3万円を払った直後の画面に、購読ボタンが出たままになりうる。**

`/connect?billing=done` は Stripe の `success_url` である。戻ってくるのは
**支払いが済んだ直後**だが、`user_metadata.subscription.status` を `active` にするのは
**webhook** であり、決済の完了と webhook の到着は**別々に起こる。**
webhook が着く前にこの画面を描くと、状態の正本はまだ空で、
画面は契約どおり「試用中」＋「標準プランにする」を出す。

**見た目の問題ではない。** 払った直後の人に購読ボタンを見せれば、
**二重に払う人が出る。** `/connect?billing=done` は「払った直後」だと分かっている
唯一の場所なので、そこで何も言わないのが問題である。

> **2026-09-03 追記 — 原因の一部が判明し、射程が縮んだ。この項目は閉じない。**
>
> 登録時は「webhook の到着待ち」だけの問題として書いた。**実際にはそれ以前に、
> 到着しても `status` に `"complete"` が書かれており、永久に購読中にならなかった**
> （2026-09-02 19:05 UTC 実測。`checkout.session.completed` の `data.object` は
> Checkout Session なので、その `status` は購読の状態ではない）。
> **待てば直る問題ではなかった。**
>
> webhook 側は修正済み（`docs/instructions/2026-09-03_cc_billing-status-fix.md`）。
> **消えたのは「永久に反映されない」部分だけ**で、残るのは本来の論点——
> **webhook が着くまでの数秒をどう見せるか**——である。
> 下の判断（1）（2）はそのまま残る。

### なぜ今回やらなかったか

スライスBU の非スコープ。契約が定めた状態は2つ（試用中／購読中）だけで、
**「払ったが、まだ反映されていない」は3つ目の状態**である。
`billing=done` を読むだけでは足りず、
「webhook を待っているのか、失敗したのか」を切り分ける設計が要る。

### 判断が要ること（**勝手に確定させない**）

1. **3つ目の状態を置くか。** `?billing=done` のときだけ購読ボタンを引っ込め、
   「確認しています」と出すのが最小の案。**ただし webhook が永久に来なければ
   永久にそう出る**ので、待つ時間の上限と、そこで何を出すかが要る
2. **待たずに確かめに行くか。** 戻り先で1度だけサーバ側から Stripe に問い合わせれば
   確実だが、**BU-D2「画面から Stripe API を叩かない」と正面からぶつかる。**
   叩くならサーバ側の1回に限り、正本は依然 webhook が書く値とする、という整理が要る

### 期限

**外部顧客に出す前に必ず潰す。** 最初の1社が購読するより前である
（`docs/spec/07_open_items.md` の「解約の導線が無い」と同じ合図）。

## dependabot の PR #80 で `integration` が落ちている（未判断・2026-09-03 登録・**merge しない**）

**merge しない。** 落ちたまま入れると、次に赤が出たときに「元から赤い」と読み流される。

> **落ち方そのものの事実は下の「integration の 401 陰性コントロールが、3回のうち3回目だけ
> 5秒でタイムアウトする」に集約した。** 同じ形が #54 でも出ており、事実を2か所に
> 散らすと片方だけ見て判断されるため。ここには #80 固有のこと（merge しない判断と
> 依存7本の内訳）だけを残す。

| 項目 | 値 |
| --- | --- |
| PR | [#80](https://github.com/shotarokajitani/sentio/pull/80) `chore(deps-dev): bump the dev-dependencies group across 1 directory with 7 updates` |
| 落ちている job | **`integration`**（5m49s） |
| run | https://github.com/shotarokajitani/sentio/actions/runs/33599722941/job/100150574107 |
| 他の5チェック | `verify` / `edge-functions` / `gitleaks` / Vercel 2件 — すべて pass |
| 最終更新 | 2026-09-02（レビュー0件・人間コメント0件） |

落ちているのは**1テストだけ**である。

```
FAIL tests/integration/pipeline-db.test.ts > パイプライン一気通貫 (S-3)
     > 陰性コントロール: anon キーからは intent を渡す以前に 401 で弾かれる
Error: Test timed out in 5000ms.
 ❯ tests/integration/pipeline-db.test.ts:281:3

 Test Files  1 failed | 12 passed (13)
      Tests  1 failed | 86 passed (87)
```

**アサーションの失敗ではなく、5秒のタイムアウトである。** `invoke("deliver-pulse", …)` の
応答を待ちきれずに落ちている。

**`integration` は同じスイートを3回走らせる。3回のうち落ちたのは3回目だけである。**

```
1379行  Test Files  13 passed (13)      ← run 1
1503行  Test Files  13 passed (13)      ← run 2
1645行  Test Files  1 failed | 12 passed (13)   ← run 3
```

3回とも `skip 0件`。**同じコード・同じ依存で、2回通って1回落ちている。**

### 分かっていないこと（**推測で埋めない**）

- **依存の更新が原因か、フレークか。** 上の3回のうち2回通っていることは
  **フレークの側を示唆する**が、断定していない。依存の更新でタイミングが
  変わって顕在化した可能性は消えていない。同じ日の dependabot PR #68
  （production-dependencies）は**全6チェック pass** している
- **7本の更新のうちどれが効いたか。** 切り分けていない。7本は次のとおり。

  | Package | From | To |
  | --- | --- | --- |
  | `@types/node` | 26.2.0 | 26.4.0 |
  | `@types/react-dom` | 19.2.4 | 19.2.5 |
  | `@vitejs/plugin-react` | 6.0.5 | 6.1.1 |
  | `eslint-config-next` | 16.3.1 | 16.3.3 |
  | `supabase` | 2.114.0 | 2.116.0 |
  | `tsx` | 4.23.12 | 4.23.13 |
  | `vitest` | 4.1.10 | 4.1.11 |

  登録時（2026-09-03 午前）は「タイミングに触りうるのは `vitest` と `supabase` CLI
  の2本」と書いたが、**同日中に実測で否定された。**

  **PR #54 は依存の更新を含まない**（`feat(ci): check:cron-jobs` を `origin/main` へ
  rebase したもの。#80 は未 merge なので deps 更新は入っていない。`package.json` /
  `pnpm-lock.yaml` への差分は0）。**そこで同じ形が出た。**

  したがって言えるのは **「依存の更新は、この症状の必要条件ではない」** までである。
  **#80 側で寄与した可能性は否定していない。** 詳細は下記
  「integration の 401 陰性コントロールが〜」に集約した
- CI の 503 フレークは過去にも観測がある
  （`docs/reports/2026-08-21_CI_503フレークの実測.md`）。同じ経路かは**未確認**

### 再実行の結果（2026-09-03・実測）

**attempt 2 は緑になった。同一コミットで結果が変わった。**

| 項目 | 値 |
| --- | --- |
| run | https://github.com/shotarokajitani/sentio/actions/runs/33599722941 （attempt 2） |
| 結論 | `conclusion=success` |
| `integration` job | https://github.com/shotarokajitani/sentio/actions/runs/33599722941/job/100600569373 → success |
| 3回の内訳 | ステップ `Run integration suite 3 times` で3回とも `Test Files 13 passed (13)` |

**落ち方は `Test timed out in 5000ms.` であり、アサーションの失敗ではない**（attempt 1）。

**これは #80 を merge する作業ではない。#80 は merge しない。**

### 判断が要ること

1. **再実行して緑になるか**を先に見る。1回で決めない
2. 再現するなら、**7本のうちどれかを外して二分する**か、
   `pipeline-db.test.ts` のタイムアウトを延ばすかを決める。
   **延ばす前に、なぜ5秒で足りなくなったかを確かめること。**
   閾値を緩めるのは、原因を見ないまま赤を消す最も安易な手である
3. dev 依存が古いまま残る期間の許容。**security advisory を含むかは未確認**

## integration の 401 陰性コントロールが、3回のうち3回目だけ5秒でタイムアウトする（未判断・2026-09-03 登録）

**事実をここ1か所に集める。散らさない。** 上の「dependabot の PR #80 で `integration` が
落ちている」と、PR #54 の CI 記録は、どちらもこの項目を指す。

### 観測（実測。1件ずつ足す）

| PR | run ID | attempt | ファイル:行 | 3回中 | 落ち方 | 再実行 |
| --- | --- | --- | --- | --- | --- | --- |
| #80 | [33599722941](https://github.com/shotarokajitani/sentio/actions/runs/33599722941) | 1 → 2 | `tests/integration/pipeline-db.test.ts:281` | **3回目** | `Test timed out in 5000ms.` | **緑**（3回とも 13 passed） |
| #54 | [33740705367](https://github.com/shotarokajitani/sentio/actions/runs/33740705367) | 1 → 2 | `tests/integration/delivery-idempotency.test.ts:168` | **3回目** | `Test timed out in 5000ms.` | **緑** |

**「赤 → 再実行 → 緑」にした回数は、この表に1行ずつ残す。**
数えないと「たまに落ちる」が「問題ない」に変わる。2026-09-03 に踏んだ形と同じである。

**3件目が同じ形で出たら、その時点で作業を止めて報告する。**
2件は偶然でありうるが、3件は形である。

### 一致している点（**これだけ**）

- どちらも **GitHub Actions の `integration`**（ローカルではない）
- どちらも **`invoke()` の応答を待つ 401 の陰性コントロール**
- どちらも **3回のうち3回目**
- どちらも **5秒のタイムアウト**。**アサーションの失敗ではない**

### 一致していない点

- **ファイルもテストも別である**（`pipeline-db` / `delivery-idempotency`）
- **依存の更新の有無が違う。** #80 は dev 依存7本の更新を含むが、
  **#54 は依存の更新を含まない**（`package.json` / `pnpm-lock.yaml` への差分0）。
  したがって **「依存の更新はこの症状の必要条件ではない」。**
  #80 側で寄与した可能性は否定していない

**同一原因かは確かめていない。**

### 調査の入口（**登録のみ。今は実行しない**）

次の2つで、**順序依存か、個別のテストの問題か**が分かれる。

1. **当該テストを単独で走らせても落ちるか。**
   単独で落ちるならテスト個別の問題、落ちないなら前の2回が残した状態か資源の問題
2. **実行順を変えると、落ちる位置が「3回目」から動くか。**
   動かないなら3回目という位置そのもの（積み上がった状態）に原因がある

**判断は書かない。** どちらを先にやるか、閾値を緩めるかどうかも決めていない。
ただし1つだけ先に置く: **タイムアウトを延ばす前に、なぜ5秒で足りなくなったかを確かめること。**
閾値を緩めるのは、原因を見ないまま赤を消す最も安易な手である。

### 混ぜないこと

**手元（ローカル）で観測したフレークと、この項目を同じ原因に丸めない。**
2026-09-03 に私のローカルで `vitest run tests/unit/` が 6 failed → 再実行で 743 全 pass
という形が出たが、**実行環境が違う**（GitHub Actions ではない。実行に103秒かかっており、
通常は約10秒）。加えて**そのとき失敗したテスト名を控えないまま再実行したため、
何が落ちたのかが分からない。** 「フレークだった」とは言えない。ここには含めない。

## Edge Function の呼び出し元を機械で守れない（未判断・2026-09-03 登録・スライスSB の SB-3-6 未充足）

> **2026-09-03 追記。cron 側も同じ問題である。**
> `check:cron-jobs`（PR #54）が入っても、この穴は塞がらない。**別項目を立てない。**
> 2つの問題に見えると、片方だけ直して塞いだ気になる。

**`check:endpoint-callers` は Next.js 専用で、Edge Function の到達性を見ていない。**

スライスSB は「`state-baselines` に呼び出し元が1つも無く、本番で一度も走っていなかった」
という事故を直すものだった。**同じ事故を再発させない番人**として、契約は SB-3-6 で
「`check:endpoint-callers` の宣言に `state-baselines` の呼び出し元を追加する」と定めた。

**これは今回充足していない。** 検査器の構造上、宣言を足すだけでは載らない。

```ts
// scripts/check-endpoint-callers.ts
function collectSources(root = "src"): SourceFile[]   // ← src/ しか走査しない
```

```ts
// tests/unit/check-endpoint-callers.test.ts:222-227
expect(s.route.startsWith("src/app/api/")).toBe(true);
expect(s.endpoint.startsWith("/api/")).toBe(true);
```

`state-baselines` は `supabase/functions/state-baselines/index.ts` にあり、
呼び出しは `_shared/dispatch.ts` からの**関数名の文字列**（`deps.invoke("state-baselines", …)`）である。
`route` も `endpoint` も上の不変条件を満たさないので、**宣言に載せた瞬間に既存テストが赤くなる。**

### いま守られている範囲と、守られていない範囲

- **守られている**: `check:caller-guard` が「デプロイ対象の全 Function が `resolveCaller` を通るか」を見る。
  ただしこれは**呼ばれ方**の検査であって、**呼ばれているか**の検査ではない
- **守られていない**: Edge Function に呼び出し元が1つも無い状態。
  **まさにこのスライスが直した形が、機械では検出できないまま残る**

### 同じ問題の別の向き — cron（2026-09-03 追記）

**`check:cron-jobs` でも塞がらない。** 検収時に「この検査器が入っていれば今日の2件は
機械が先に見つけた」という見立てがあったが、**実測で否定された。**

この検査器が突き合わせるのは **宣言（`docs/checklists/cron-jobs.yml`）× DB の `cron.job`**
の2つだけである。今日踏んだのは**どちらにも最初から居ない**という別の向きなので、
突合しても何も出ない。当時の main の状態で `compareCronJobs` に実データを流した結果:

```
宣言: sync-connections / dispatch-daily / dispatch-weekly
実物: dispatch-daily / dispatch-weekly / sync-connections
findings: []
```

CI（run 33733352340・`ci.integration`）でも同じで、
`check:cron-jobs — cron.job 3件が宣言と一致` と出て緑になった。

陰性コントロールは効いている（宣言したジョブが DB から消えれば `missing`、
宣言外のジョブが DB に居れば `undeclared`）。**射程が違うだけで、検査器は間違っていない。**

| 今日踏んだ形 | `check:cron-jobs` | `check:endpoint-callers` | `check:caller-guard` |
| --- | --- | --- | --- |
| `state-baselines` — 実装はあるが呼び出し元が無い | 射程外（cron の話ではない） | 射程外（`src/` しか走査しない） | 射程外（呼ばれ方の検査） |
| `retention-purge` — 実装はあるが cron が張られていない | **射程外**（両側に居ないので一致） | 射程外 | 射程外 |

**つまり「デプロイされているのに起動経路を持たない Function」を見る検査器は、
いまリポジトリに1本も無い。** 上の「判断が要ること」は、cron 経路も対象に含めて決める。

**`check:cron-jobs` が果たす役割は別にある。** これから張る cron の**宣言漏れ**は止まる。
`retention-purge` の cron を張ったとき `cron-jobs.yml` への追記を忘れれば、
実DB側にだけ現れて `undeclared` で赤くなる（陰性コントロールで実測済み）。
**今ある穴は塞がないが、これから開ける穴は塞ぐ。**

### 判断が要ること（**勝手に確定させない**）

1. **`check:endpoint-callers` を Edge 対応に広げるか、専用の検査器を新設するか。**
   広げるなら `EndpointSpec` に種別を持たせ、`supabase/functions/` も走査し、
   不変条件テストを種別ごとに分ける。新設するなら `ci-coverage.yml` への宣言と
   CI への搭載、陽性・陰性コントロールがセットで要る（`.claude/rules/ci-coverage.md`）
2. **`state-*` のうち、呼び出し元を持つべきものはどれか。**
   `state-narratives` / `state-summary` / `state-memory-packet` は現在も呼び出し元が無い。
   これらは「いま決めない」として別途登録済みで、**検査器を先に作ると、
   その未判断が機械的に赤として現れる。** 順序をどちらにするかも判断のうち

### 着手の合図

**次に Edge Function を1本足すとき。** そのとき呼び出し元を書き忘れても、
いまは誰も気づかない。SB と同じ事故がもう一度起きる。
