# セッション記録 — 2026-09-03（検収側）

課金を本番で立ち上げ、Finding が0件である理由を特定し、放置されていた PR を掘り出した1日。
**この記録の主張はすべて実測に紐づく。** 推測は「未検証」と明記した。

---

## 1. 完了したこと

### 1-1. 課金が本番で動いた（スライスBU・PR #84）

朝の時点で「API はあるが押す場所が無い」状態だった。

```
$ grep -r "billing" src/
src/app/api/billing/checkout/route.ts
src/app/api/billing/webhook/route.ts
（UI からの参照は 0件）
```

`/connect` に「プラン」の節を足し、Preview で通しを実行した。

```
19:05:18 UTC  POST /api/billing/checkout  200
19:05:44 UTC  POST /api/billing/webhook   200   ← 署名検証を通過
19:05:55 UTC  GET  /connect               200
```

本番デプロイ後、署名なしで叩いて **401**（`{"error":"invalid signature"}`）を確認。
500 ではないので `STRIPE_WEBHOOK_SECRET` が読めており、**認証が本番で効いている実測**になる。

**画面は「標準プラン・購読中」に変わり、購読ボタンが消えた。**

### 1-2. 購読の status が永久に `active` にならないバグ（PR #84 追加コミット）

Preview の通しで書き込まれた値:

```json
"subscription": { "status": "complete", "plan_id": "standard", ... }
```

`webhook/route.ts:67-71` が `object.status` をそのまま入れていた。
**`checkout.session.completed` の `data.object` は Checkout Session** で、その `status` は
`open` / `complete` / `expired` の3値。**購読の状態ではない。**

`connect-client.tsx:277` も `plan.ts:37` も `"complete"` を通さないので、
**払っても購読ボタンが消えず、枠も増えない。時間の問題ではなかった。**

**テストが緑だった理由も同じ形だった。** `tests/unit/billing-webhook.test.ts` の
フィクスチャが `checkout.session.completed` に `status: "active"` を入れていた。
**Checkout Session に `active` は入らない。実装に合わせて書かれ、実物に合わせて書かれていなかった。**

フィクスチャを実物の形に直した時点で、既存テストが赤くなった。

```
AssertionError: expected 'complete' to be 'active'
Tests  7 failed | 14 passed (21)
```

### 1-3. State 層を毎日回す（スライスSB・PR #85）

アーキテクチャは Ingest → State → Sense → Act だが、**動いている系から State が丸ごと抜けていた。**

```
run-sense/index.ts:52   → scan
run-sense/index.ts:132  → investigate
_shared/dispatch.ts:107 → run-sense → deliver-*

state-baselines を呼ぶ行は、リポジトリのどこにも無い
```

本番の `baselines` は1行だけ（`revenue` / `is_established: false` / 最終更新 08-27）。
`schedule_interval` を書くコードは 08-31 に足されていたが、**一度も走っていなかった。**

`dispatch-daily` の中、`run-sense` の直前に `state-baselines` を入れた。
**新しい cron は張らない**（契約CD の CD-D2 を守る）。ディスパッチャ内に置くことで
State → Sense の順序が構造的に保証される。

呼び出し順を**配列で**突き合わせたので、実装前に赤くなった。

```
FAIL SB-1-1: expected [ 'run-sense', 'deliver-pulse' ] to deeply equal [ Array(3) ]
```

**集合で見ていたら「3本とも呼ばれている」で緑になっていた。**

### 1-4. 連携取り消しの実測（§7 の反証・D-3 の着手条件）

梶谷さんが Google アカウント設定から Sentio の連携を取り消した（**意図的な実験**）。

```
取り消し  2026-09-03 01:14 UTC（10:14 JST）
検知      2026-09-03 06:00:03 UTC（15:00 JST）
遅延      4時間46分
```

```
provider          status    revoked_at                  last_refresh                expires_at
google_calendar   revoked   2026-09-03 06:00:03.841+00  2026-09-03 00:00:03.929+00  01:00:02+00
google_calendar   revoked   2026-09-03 06:00:04.006+00  2026-09-03 00:00:05.370+00  01:00:03+00
```

**公開中のポリシー §7 は「最大1時間程度」と書いている。実測は4時間46分。**

`expires_at` が取り消しの14分前に既に期限切れなので、15:00 の回で確実に refresh に入る状態だった。
**遅延の理由は cron の間隔（6時間おき）だけである。**

**2行とも `revoked` に倒れた。** `last_refresh` が1.4秒差、`revoked_at` が0.165秒差 —
同じ cron の同じループで、会社ごとに独立して処理されている。

これで契約D の停止点「`revoked` の実例を人間が確認するまで D-3 に着手禁止」が満たされた。

---

## 2. 分かったこと — Finding が0件である構造的な理由

**データ量ではなかった。**

### 2-1. `metrics.revenue` を書く取り込み経路が1つも無い

| 経路            | 実際に書いている `metrics`                    | 出所                            |
| --------------- | --------------------------------------------- | ------------------------------- |
| Google Calendar | `{ title, attendees }`                        | `sync-connections/index.ts:238` |
| freee           | `{ description, amount, deal_type }`          | `sync-connections/index.ts:325` |
| 入出金CSV       | `{ description, amount, direction, balance }` | `csv/ingest/route.ts:184-189`   |

読む側（`scan.ts:134` / `state-baselines/index.ts:49`）は `metrics.revenue` を見る。
**書く側は全部 `amount`。59行のCSVを入れても、走査1は1件も候補を出せない。**

**評価スイートが緑だった理由も同じ。**
`scripts/generate-synthetic-company.ts:68` が `metrics: { revenue, amount: revenue, ... }` と
**両方書いている。** 合成会社だけが `revenue` を供給していた。

### 2-2. 走査8本のうち、本番で発火しうるのは実質0本

| #   | 走査             | 本番での状態                                              |
| --- | ---------------- | --------------------------------------------------------- |
| 1   | 乖離（revenue）  | **入力が無い。永久に0**                                   |
| 2   | 期限超過         | `is_overdue` を書く経路が無い                             |
| 3   | 外部（S0）       | 取り込み経路が無い                                        |
| 4   | 監視             | 取り込み経路が無い                                        |
| 5   | 指標の連続悪化   | communication / web / attendance のどれも取り込んでいない |
| 6   | 途絶（会社全体） | `schedule_interval` ベースラインの行が存在せず、回らない  |
| 7   | 途絶（シリーズ） | 対象にはなるが、条件を満たしていない                      |
| 8   | 伸長（シリーズ） | 同上                                                      |

**7・8 は閾値のせいではない。** 実測（CC）:

- `transaction` は系列が4件以上あり対象になっている
- 途絶: 最後の取引が3日前、平常が5日間隔 → **系列は生きている**
- 伸長: 間隔が 1〜29日と散らばり、**単調増加ではない**

**検知すべき変化が起きていない。** `SERIES_MIN_INTERVALS` を下げる根拠にはならない。

### 2-3. したがって問いが変わった

顧客0号（自社）は健全で、いま異常が無い。**異常が無い会社に異常を報告させることはできない。**

> **何も起きていない会社に、Sentio は何を言うのか。**

品質基準は「**自社の状態が一枚の絵として浮かぶか**」であって「異常が報告されるか」ではない。
**いまのパルスは異常検知の器しか持たず、状態を記述する器を持っていない。**

そして構造的な原因がもう1つある。

```
run-sense/index.ts:129
if (scanResult.candidates.length > 0) {   ← 候補0件なら Investigator は呼ばれない
```

**異常が無い日、LLM は一度も会社を見ていない。**
CLAUDE.md が「『翻訳』が後半」と書いているその後半が、**異常の有無に依存している。**

---

## 3. 今日の全ての問題が同じ形をしていた

```
評価スイート        本番で走っていない別実装を6週間測っていた（既知）
合成会社            本番が生成しないキー（revenue）を供給して緑だった
webhook のテスト    フィクスチャが実装の写しで、実物と違っていた
state-baselines     実装はある。呼び出し元がリポジトリのどこにも無い
retention-purge     実装はある。cron が張られていない
check-cron-jobs     検査器はある。PR が3日間 merge されず放置
oauth-consent-screen.md  規則はある。未merge のブランチにしか無い
監視スクリプト      comm の process substitution が効かず、無言でタイムアウト
METRIC_ALLOWLIST    載せないとキー名すら出力されない（2回踏んだ）
```

**共通するのは「何も出ない」が「問題ない」に読めることである。**

- 検査器が空を返す → 緑に見える
- Monitor が黙る → 成功に見える
- allowlist に無いキー → 出力から消える
- cron が張られていない → 突合しても何も出ない

**鳴らない監視は監視ではない。**

もう1つの共通点は、**「緑」が実物の保証になっていなかった**こと。
テストのフィクスチャを**実装に合わせて**書けば、実装が間違っていてもテストは同じ間違いをする。
**自分の答えを写して丸をつけるのと同じである。**

---

## 4. 放置されていた PR

**CI 全緑・conflict なし・人間のコメント0件・レビュー0件・止めた理由の記録なし。**

| #   | 題名                                                       | 最終更新   | 停滞 | main に無い追加ファイル                      |
| --- | ---------------------------------------------------------- | ---------- | ---- | -------------------------------------------- |
| #50 | 「出力が薄い」原因が②データ量だった切り分け                | 2026-08-29 | 5日  | 報告書1本                                    |
| #54 | cron.job の恒久検査器 `check:cron-jobs`                    | 2026-08-31 | 3日  | 検査器・宣言・テスト                         |
| #83 | 承認を記録し、同意画面の変更が再審査を引くことを規則にする | 2026-09-02 | 1日  | `.claude/rules/oauth-consent-screen.md` ほか |
| #80 | dependabot（dev-dependencies）                             | 2026-09-02 | 1日  | **integration が落ちている**（緑ではない）   |

**#50 は作成6秒後の bot コメント以降、一度も動いていない。**

### 4-1. #50 の結論は反証されている

8/29 の結論は「原因は②データ量。次は2本目のデータ源」だった。
**提案どおり 8/31 に2本目（入出金CSV・59行）を入れた。Finding は0のままだった。**

**間違っていたのではなく、その後の出来事に反証された。**
当時としては妥当な読みで、実際にそのとおり手を打ち、出なかったので今日その先を掘った、という順序である。

### 4-2. #54 は今日の2件を捕まえない（実測）

**私（検収者）は「この検査器が入っていれば今日の2件は機械が先に見つけた」と言った。誤りだった。**

```
findings: []                                   ← retention-purge に cron が無い今の main で緑
陰性（宣言を消す）:      missing で赤くなる
陰性（DBに余分がある）:  undeclared で赤くなる
```

この検査器が見ているのは**宣言と DB の cron.job のずれ**。今日踏んだのは
**「関数はあるが、宣言にも DB にも最初から居ない」**という別の向きで、突合しても何も出ない。

**射程が違うだけで、検査器は間違っていない。** ただしその限界は明記が要る。

---

## 5. 決めたこと

| #     | 論点                         | 決定                                                                                                                                          |
| ----- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | ポリシー §7「最大1時間程度」 | **(a) 文言を実態（最大6時間程度）に直す。** cron は触らない。取り消し後は次の cron まで取り込みも起きないので、表示が古いこと以外の実害が無い |
| **2** | cron の間隔を縮めるか        | **§7 のためには縮めない。** ただし「取り込みの鮮度」という別の価値はあるので、別項目として残す。①の設計時に一緒に判断する                     |
| **3** | D-3（30日削除）の着手        | **着手可。** `revoked` の実例が2件でき、人間が確認した                                                                                        |
| **4** | `retention-purge` の cron    | **新しい `cron.schedule` を張る。** ディスパッチャに相乗りさせない（会社ごとの引数を取らないため、CD-D2 の理由が当てはまらない）              |
| **5** | 放置PR の順番                | **#54 → #86 → #83 → #50。** 全部 `07_open_items.md` を触るので、緑になった順に入れて都度 rebase                                               |
| **6** | #50 の扱い                   | **内容は書き換えず merge。** 冒頭に「反証された」注記を1行だけ足す。当時の判断の記録として残す                                                |

### 決めなかったこと（**勝手に確定させない**）

- `revenue` を何と定義するか（`amount` は出金が負の符号付きで、読み替えでは済まない）
- 走査2〜5 を残すか消すか
- `trialing` を購読中と見なすか
- `?billing=done` 直後に3つ目の状態を置くか
- `check:endpoint-callers` を Edge 対応に広げるか、専用の検査器を新設するか

---

## 6. ローンチまでに要るもの

**外部の顧客1社に、金を取って使ってもらう**を基準にすると5件。

|                              | 状態                                            | 分量     |
| ---------------------------- | ----------------------------------------------- | -------- |
| **① 毎朝のメールに読む理由** | **原因は判明。設計はこれから**                  | 読めない |
| **② アカウント削除API**      | 未着手。**利用規約で公開済みの約束**            | 読める   |
| **③ 連携解除が §6 を満たす** | **実装中**（D-3）。経路1・2は既に実装済みと判明 | 読める   |
| **④ 解約の導線**             | 未着手。購読したら止められない                  | 読める   |
| **⑤ `?billing=done` 直後**   | 未判断。**優先度は下がった**（下記）            | 小さい   |

**②③ は「作っていない」ではなく「書いてあるのに無い」。** 外部に出した瞬間に約束違反になる。

**① が無ければ、他を全部やっても出す意味がない。** 3万円を払う理由が無いため。

### ⑤ の優先度が下がった根拠

```
19:05:18  checkout セッション作成
19:05:44  webhook 到着          ← 画面に戻る11秒前
19:05:55  /connect 表示
```

**webhook が競争に勝っていた。** n=1 なので断定はしないが、「二重に払う人が出る」確率は
思っていたより低い。

---

## 7. 未完了・持ち越し

### 私（検収者）

- **明朝 9/4 07:00 JST の `schedule_interval` を確認する。**
  期待値: 2社ぶん `is_established: true` / `stats->>'count'` = 21。
  `revenue` は行の `updated_at` だけ当日に更新され `is_established` は false のまま
  （true になったら SB-D5 違反の検知）
- **①（毎朝のメールに読む理由）の契約を書く。** 論点はコストの上限
  （`フルハーネス日次上限 = 10` は暫定値）と、何も無い日に嘘を書かせない担保
- **KIJI の資料が読めていない。** PDF レンダリングツールが無く、Linux 側のシェルも
  タイムアウトしている。価格・契約期間・データの粒度・API の形が分からないと、
  Sentio 側の工数が見積もれない

### 梶谷さん

- **`/connect` から Google カレンダーを再連携する。**
  `revoked_at` が 2026-09-03 06:00 UTC で入っているので、**D-3 が入ると 10月3日に
  この2社のカレンダー由来イベント（予定22日ぶん・`schedule_interval` の土台）が自動削除される。**
  再連携すれば `revoked_at` が NULL に戻り対象から外れる
- **bypass secret の再発行**（Stripe の配信ログに URL ごと残っている。使い捨ての約束）
- **Preview に入れたテスト鍵の扱い**（他の PR の Preview にも効く）

### CC

- PR #54 → #86 → #83 → #50 の順に merge
- PR 2（D-3 実装 ＋ `retention-purge` の cron）
- `docs/checklists/cron-jobs.yml` の文字化け（`живой`）を #54 で修正
- #80（integration が落ちている dependabot PR）を `07_open_items.md` に登録

---

## 8. 私（検収者）の誤り

**3回、根拠を確かめずに断定した。**

1. **`.claude/rules/oauth-consent-screen.md` を「存在しない」と言った。**
   現在のブランチだけ見て「実体は0件」と結論した。実際は未merge のブランチにあった
2. **「cron を1時間以下にすると検知が止まる」と言った。**
   `EXPIRY_BUFFER_MS = 5分` を見落としていた。正しくは「下限がある」であって、
   1時間おきなら成立する。**選択肢を1つ潰しかねない誤りだった**
3. **「#54 が入っていれば今日の2件は機械が先に見つけた」と言った。**
   検査器の射程を確かめずに言った。実測では捕まえない

**手順の出し方でも時間を無駄にさせた。** URL を「該当の行 → ⋯」と書き、
実際に押せる形で確かめていなかった。**以後、URL は完全な形で1本だけ出す。**

---

## 9. この日から残る規律

> **「何も出ない」を「問題ない」と読まない。**

検査器・監視・allowlist・突合 — 今日出た穴は全部この形だった。
**鳴らない仕組みは、鳴らないこと自体を疑う。**

> **フィクスチャは実物に合わせる。実装に合わせない。**

実装に合わせて書けば、実装が間違っていてもテストは同じ間違いをする。
**フィクスチャを実物の形に直した瞬間に赤くなるなら、それは本番の事故の再現である。**

> **実装があることと、動いていることは別。**

`state-baselines` / `retention-purge` / `check-cron-jobs` / 規則ファイル。
**4件が今日1日で出た。** 呼び出し元・cron・merge のどれかが欠けていた。
